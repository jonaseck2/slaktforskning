{
  "expectations": [
    {
      "text": "The save handler has a try/catch block where the catch logs to console.error AND calls toast.error() to notify the user",
      "passed": false,
      "evidence": "Lines 42-46: plain await with no try/catch — errors silently swallowed"
    },
    {
      "text": "Imports and calls useToast for error handling in the save catch block",
      "passed": false,
      "evidence": "No useToast import anywhere in the file"
    },
    {
      "text": "Has NO local 'declare const window' type declaration",
      "passed": false,
      "evidence": "Lines 26-28: declare const window: Window & { api: Record<string, Record<string, (...args: unknown[]) => Promise<unknown>>> } — forbidden pattern re-appeared"
    },
    {
      "text": "All user-visible strings (button labels, placeholder, heading) use $t() not hardcoded text",
      "passed": true,
      "evidence": "All strings via $t(): addNoteTitle, notes, notesPlaceholder, cancel, save"
    },
    {
      "text": "Any scoped CSS uses var(--color-*) or var(--font-*) variables, not hardcoded hex values",
      "passed": true,
      "evidence": "No scoped styles at all"
    }
  ],
  "summary": { "passed": 2, "failed": 3, "total": 5, "pass_rate": 0.4 }
}

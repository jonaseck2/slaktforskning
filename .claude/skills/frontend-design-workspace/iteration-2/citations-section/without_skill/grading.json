{
  "expectations": [
    {
      "text": "All async API calls are wrapped in try/catch blocks where the catch logs to console.error AND calls toast.error() to notify the user",
      "passed": false,
      "evidence": "load() at line 50-52 has no try/catch at all. No error handling anywhere in the component. Errors silently swallowed."
    },
    {
      "text": "Has NO local 'declare const window' type declaration",
      "passed": true,
      "evidence": "Not present — uses window.api directly"
    },
    {
      "text": "Imports and calls useToast; catch block calls toast.error(t('errors.loadFailed')) or equivalent",
      "passed": false,
      "evidence": "No useToast import, no catch blocks anywhere in the component."
    },
    {
      "text": "All user-visible strings use $t() not hardcoded Swedish or English text",
      "passed": true,
      "evidence": "All strings via $t(): citations.none, citations.source, sourceDetail.page, citations.confidence, confidenceLevels.* (dynamic). No hardcoded labels."
    },
    {
      "text": "Any scoped CSS uses var(--color-*) or var(--font-*) variables, not hardcoded hex values like #2c3e50 or #333",
      "passed": false,
      "evidence": "Lines 70-73: confidence badge colors all hardcoded hex: #fef2f2/#991b1b (0), #fff7ed/#92400e (1), #fefce8/#854d0e (2), #f0fdf4/#166534 (3)"
    }
  ],
  "summary": { "passed": 2, "failed": 3, "total": 5, "pass_rate": 0.4 }
}

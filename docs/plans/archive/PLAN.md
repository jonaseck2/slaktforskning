# Archived Plan Context: Släktforskning

This file preserves the original vision, design goals, tech decisions, and full implementation history that was condensed out of `docs/PLAN.md` during the 2026-04-04 cleanup. Kept for reference; not a living document.

---

## Vision

A local-first, cross-platform desktop genealogy application that gives researchers full control of their data while being natively agent-friendly — allowing AI assistants to read, write, and reason about family tree data without a GUI.

## Design Goals

1. **Local-first** — All data stored on the user's machine in SQLite. No cloud accounts, no subscriptions, no data leaving the device unless the user exports it.
2. **Multi-window** — Desktop-class UX with multiple simultaneous windows into the same database (e.g., person detail + family view side-by-side).
3. **Cross-platform** — Runs on macOS, Windows, and Linux from a single codebase.
4. **Agent-friendly** — A built-in MCP server lets AI agents perform full CRUD operations on the database without the UI, enabling agentic workflows like automated data entry, research assistance, and GEDCOM import.
5. **Source-first** — Every claim in the database should be traceable to a source. The app warns on unsourced entities and provides citation affordances everywhere — but does not block entry when a source isn't yet at hand.
6. **Research-grade** — Data model follows the Genealogical Proof Standard: Source → Citation → Assertion. Preserves uncertainty, conflicting evidence, and original source text.

## Tech Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Desktop framework | Electron 41 | Rich ecosystem, agent-friendly (Playwright/DevTools), cross-platform, mature |
| Frontend | Vue 3 + Pinia | Lightweight, good TS support, less opinionated than React for a desktop app |
| Build system | Electron Forge + Vite | Official Electron tooling, fast HMR, good native module support |
| Database | SQLite via node-sqlite3-wasm | Zero-config, single-file, WASM-based — no native rebuild needed for Electron vs system Node |
| Agent interface | MCP (stdio transport) | Standard protocol for AI tool use; Claude, GPT, etc. can call it directly |
| Language | TypeScript throughout | Single language for main process, renderer, API layer, and MCP server |
| i18n | vue-i18n | Swedish default locale, English fallback, persisted to localStorage |

### Alternatives Considered

- **Tauri** — Smaller binary, Rust backend. Rejected: less mature agent tooling (no Playwright for WebView2), smaller ecosystem.
- **React** — More popular but heavier; Vue's Composition API fits the desktop UI better.
- **better-sqlite3** — Native C++ addon with excellent performance, but requires recompilation when switching between system Node and Electron's Node fork. Replaced with node-sqlite3-wasm to eliminate the constant rebuild problem.
- **Prisma / Drizzle ORM** — Rejected in favor of raw SQL for simplicity and full control over genealogy-specific queries.
- **PostgreSQL** — Rejected: local-first goal means no server process. SQLite is the right fit.
- **GEDCOM-X interchange** — GEDCOM-X is the conceptual inspiration for the relationship model, but it is not widely supported by any Swedish genealogy platform. GEDCOM 5.5.1 is the interchange standard; the internal model roundtrips to/from it.

## Architecture

```
src/api/     → Pure business logic (zero Electron deps)
src/main/    → Electron main process (windows, DB, IPC)
src/preload/ → Context bridge (renderer ↔ main)
src/renderer/→ Vue 3 UI (Swedish default locale)
src/mcp/     → MCP server (standalone, same API layer)
```

**Key principle:** `src/api/` is the single source of truth. Both the Electron IPC handlers and the MCP server call the same functions. All api/ functions take a `Database` instance as their first argument (dependency injection, no singletons).

---

## Full Implementation History

### Done (v0.1.0 — Foundation)
SQLite + API layer + Electron shell + MCP server (14 tools) + unit/E2E tests. Migrated from better-sqlite3 to node-sqlite3-wasm.

### Done (v0.2.0 — Genealogy Data Entry UI)
Full form-based data entry: PersonPicker, DateInput, EventForm, EventList, CitationForm components. Detail views for persons, families, sources. 22 GEDCOM event types, date types, confidence levels.

### Done (v0.2.1 — Global Search)
`/search` route with SearchView across Persons, Families, Sources. Sidebar search input.

### Done (v0.2.2 — MCP UI Tools)
HTTP bridge (port 19241) + `ui_screenshot`, `ui_navigate`, `ui_get_dom`, `ui_click`, `ui_execute_js` MCP tools.

### Done (v0.2.3 — Swedish i18n + MCP Parity)
vue-i18n with ~180 strings (SV default, EN fallback). Expanded MCP from 14 → 34 tools matching full IPC surface.

### Done (v0.3.0 — Relationships + Evidence + Add Related Person)
GEDCOM-X relationship model, citation affordances, Add Related Person modal. See `docs/plans/archive/2026-04-02-v030-evidence-and-add-related.md`.

### Done (v0.3.1 — GEDCOM-X Name Parts + Person Identifiers)
Name prefix/suffix/patronymic/qualifier columns; `person_identifiers` table with typed external IDs. See `docs/plans/archive/2026-04-02-gedcomx-name-parts-and-identifiers.md`.

### Done (v0.3.2 — PersonDetailView UX Improvements)
Inline sex editing; clickable name rows; relationship delete button; AddRelatedPersonModal New/Existing toggle. See `docs/plans/archive/2026-04-02-person-detail-ux.md`.

### Done (v0.4.0 — Places)
Full places layer: API, IPC, 6 MCP tools, PlacePicker, PlacesView + PlaceDetailView. See `docs/plans/archive/2026-04-02-places.md`.

### Done (v0.4.1 — Minor Fixes & UX Consistency)
Inline-edit polish across PersonDetailView, RelationshipDetailView, PlaceDetailView. See `docs/plans/archive/2026-04-03-detail-view-ux-consistency.md`.

### Done (v0.5.0 — Visualization)
Pedigree/Hourglass/Timeline SVG charts; click-to-navigate; `chartLayout.ts` + `chartData.ts` utilities. See `docs/plans/archive/2026-04-03-visualization-impl.md`.

### Done (v0.5.1 — UI Consistency Polish)
EventList rows clickable (no Edit button); PlacePicker + PersonPicker get `width: 100%`; PlaceDetailView parent place constrained to grid column. See `docs/plans/archive/2026-04-03-ui-consistency.md`.

### Done (v0.5.2 — Sourcing & Citation Consistency)
`CitationBadge` component; `getCitationsForPerson/Relationship/Place` API + IPC + MCP; Cite button + badge in detail view headers; badges in PersonsView list. See `docs/plans/archive/2026-04-03-sourcing-and-citations.md`.

### Done (v0.5.3 — Place Address Fields)
`street`, `postal_code`, `city`, `country` columns on `places`; MCP `add_place`/`update_place` updated; PlaceDetailView Address section; PlacePicker shows postal_code/city subtitle. See `docs/plans/archive/2026-04-03-place-address.md`.

### Done (v0.5.4 — Tilltalsnamn)
`preferred_name` on `person_names`; `getDisplayGivenName()` helper; underline rendering in PersonDetailView, PersonsView, PersonPicker. See `docs/plans/archive/2026-04-03-preferred-name.md`.

### Done (v0.5.5 — Search Across All Name Records)
`searchPersons` searches all `person_names` rows via EXISTS subquery. See `docs/plans/archive/2026-04-03-name-search.md`.

### Done (v0.5.6 — PersonName Component & Consistent Underline)
`PersonName.vue` + `nameUtils.ts` unify tilltalsnamn underline rendering across all views and SVG charts. See `docs/plans/archive/2026-04-03-person-name-component.md`.

### Done (v0.6.0 — GEDCOM Import/Export)
GEDCOM 5.5.1 parser, date parser, importer (INDI/FAM/SOUR), exporter, IPC, MCP tools. 31 unit tests. See `docs/plans/archive/2026-04-03-gedcom.md`.

### Done (v0.6.2 — Genney Import Profile)
`swedishPlace.ts`, `swedishNames.ts`; `profile: 'genney'` option in importer; patronymic detection, Swedish place hierarchy. 231 unit tests. See `docs/plans/archive/2026-04-03-genney-import.md`.

### Rejected (v0.6.5/v0.6.6 — Genney Export Roundtrip)
Investigated and rejected 2026-04-03. Genney 4.1's proprietary tags are stripped on re-export; true lossless roundtrip is impossible. See `docs/plans/archive/2026-04-03-genney-export-roundtrip.md`.

### Done (v0.6.3 — Database Switcher)
`settings.ts`; `switchDatabase()`; IPC `db:*`; `DatabaseView.vue`; `get_current_database`/`switch_database` MCP tools. See `docs/plans/archive/2026-04-03-database-switcher.md`.

### Done (v0.6.4 — Extended GEDCOM Roundtrip)
Lossless GEDCOM 5.5.1 cycle with extension tags. 29 new unit tests (262 total). See `docs/plans/archive/2026-04-03-gedcom-extended.md`.

### Done (v0.6.5 — Chart Enhancements)
Pedigree to 5 generations; hourglass to 4+3 levels; zoom/scroll with cursor anchor; spouses in hourglass. See `docs/plans/archive/2026-04-03-chart-enhancements.md`.

### Done (v0.6.6 — Collapsible Visualisation Nodes)
`CollapseButton` + pruning logic; collapse/expand ancestor subtrees, children, spouses; 14 new unit tests. See `docs/plans/archive/2026-04-03-collapsible-viz.md`.

### Done (v0.6.7 — Per-Node Descendant Collapse)
↓ button on every descendant node with children; collapse-aware `leafCount`. 4 new unit tests (288 total). See `docs/plans/archive/2026-04-03-descendant-collapse.md`.

### Done (v0.6.8 — Tilltalsnamn + Smeknamn separation)
`nickname` column; `fullNameParts` renders nickname in double quotes; GEDCOM import/export updated; 12 new unit tests (300 total). See `docs/plans/archive/2026-04-03-tilltalsnamn-and-smeknamn.md`.

### Done (v0.6.9 — Genney Derby Import)
`DerbyExtractor.java`; `transform.ts`; Docker orchestration; GEDCOM fallback; 38 new unit tests (335 total). See `docs/plans/archive/2026-04-04-genney-derby-import.md`.

### Fix — Stale IPC database reference after switchDatabase
See `docs/plans/archive/2026-04-03-ipc-stale-db.md`.

### Fix — GEDCOM import timeout + Genney preferred name asterisk
See `docs/plans/archive/2026-04-03-gedcom-import-performance.md`.

### Fix — GEDCOM import CPU saturation (statement cache)
See `docs/plans/archive/2026-04-03-gedcom-import-cpu.md`.

### Fix — GEDCOM import: couple subtype always 'unknown'
See `docs/plans/archive/2026-04-03-gedcom-couple-subtype.md`.

### Fix — Nickname insertion position + asterisk notation in UI
See `docs/plans/archive/2026-04-04-nickname-display-and-asterisk-ui.md`.

### Fix — Genney import CPU saturation (per-row db.prepare)
See `docs/plans/archive/2026-04-04-genney-cpu-saturation.md`.

### Fix — Genney .backup extraction: fflate instead of unzip subprocess
See `docs/plans/archive/2026-04-04-genney-windows-paths.md`.

### Fix — Genney EVENT_PLACE column names wrong; REMARK.TEXT→NOTE
See `docs/plans/archive/2026-04-04-genney-column-bugs.md`.

### Fix — import_gedcom silently swallowed .backup files; add import_genney MCP tool
See `docs/plans/archive/2026-04-04-import-genney-mcp-tool.md`.

### Fix — QualityView: auto-run checks on mount, removed manual run button

### Fix — Relationship EventList: wire CitationBadge on event rows

### Fix — Reactive quality badge: contextBridge onDataChanged pattern
See `docs/plans/archive/2026-04-05-fix-reactive-quality-badge.md`.

### Fix — ResearchTasksView: table styling, person column, reactive badge
See `docs/plans/archive/2026-04-05-research-tasks-view-fixes.md`.

### Fix — Consistent name rendering: nickname + preferred_name in all views
See `docs/plans/archive/2026-04-05-consistent-name-rendering.md`.

### Fix — Circle chart zoom: flex-shrink, center buttons, curved text
See `docs/plans/archive/2026-04-05-circle-chart-zoom-fixes.md`.

### Fix — v0.20.9: WASM OOM after large GEDCOM import
See `docs/plans/archive/2026-04-05-fix-wasm-oom-after-large-gedcom-import.md`.

### Fix — v0.20.10: Systematic WASM heap leak fix — queryOne/queryAll/runSql helpers

### Fix — v0.22.4: PersonsView UI lockup — correlated subqueries + indexes
See `docs/plans/archive/2026-04-05-fix-personsview-lockup.md`.

### Fix — v0.23.1: Remove 500-ancestor limit from Ancestor Book export

### Fix — v0.23.2: ReportsView full-width + auto-fit zoom controls

### Fix — v0.23.3: Circle chart gen 5-6 radial text visibility

### Fix — v0.23.4: Chart birth/death date formatting; BOX_H 44→54; circle fixes

### Fix — v0.23.5: Circle gen 6 colors; Reports auto-use focal person

### Fix — v0.23.7: AncestorBook gen 6 blank — fetchPedigreeTree generations fix

### Fix — v0.24.1: Quality checks CPU saturation on large trees
See `docs/plans/archive/2026-04-06-checks-performance.md`.

### Fix — v0.25.2: Genney SPLACE address fields stored in place columns
See `docs/plans/archive/2026-04-06-genney-address-fields.md`.

### Fix — v0.26.1: Pedigree/hourglass buttons clipped at SVG boundary
See `docs/plans/archive/2026-04-06-chart-button-clip-fix.md`.

### Fix — v0.26.2: E2E flaky tests — AbortError retry, mutating() wrappers
See `docs/plans/archive/2026-04-06-e2e-fix-flaky-tests.md`.

### Fix — v0.26.3: formatFullName() in nameUtils for all reports
See `docs/plans/archive/2026-04-06-full-name-rendering.md`.

### Fix — v0.37.7: PersonsView startup CPU — two-pass query optimization
See `docs/plans/archive/2026-04-07-personsview-startup-cpu.md`.

### Investigation — GEDCOM Citation Roundtrip: keep current behavior (no changes)
See `docs/plans/archive/2026-04-05-gedcom-citation-roundtrip.md`.

### Refactor — TypeScript window.api typing: api.d.ts, removed unsafe Record<>
See `docs/plans/archive/2026-04-07-typescript-api-types.md`.

### Refactor — User-facing error toasts: useToast + ToastNotification
See `docs/plans/archive/2026-04-07-error-notifications.md`.

### Refactor — i18n coverage: personPanel.*, nav.navigate, hardcoded strings
See `docs/plans/archive/2026-04-07-i18n-coverage.md`.

### Refactor — CSS color variables: 18 palette vars, replaced 70+ hex values
See `docs/plans/archive/2026-04-07-css-color-variables.md`.

### Refactor — BaseModal component: slot-based shell, migrated 15 modals
See `docs/plans/archive/2026-04-07-base-modal-refactor.md`.

---

## 2026-04-11: Competitor Gap Analysis → Track Implementation

### Research — Competitor Gap Analysis
See `docs/plans/2026-04-11-competitor-gap-analysis.md`.

### v0.52.0 — C2: Person Timeline View
Chronological event timeline with gap detection, date type handling, age calculation, edit-on-click.
Part of Track C: `docs/plans/2026-04-11-track-c-core-polish.md`.

### v0.53.0 — D1: MCP Report Generation Tools
6 higher-level MCP tools (get_person_summary, get_family_unit, get_ancestor_tree, get_place_history, get_research_gaps, get_timeline) + 17 unit tests.
Part of Track D: `docs/plans/2026-04-11-track-d-mcp-agent-story.md`.

### v0.54.0 — B1: Media Viewer Redesign
MediaView gallery with lightbox, entity linking, thumbnails, keyboard navigation.
Part of Track B: `docs/plans/2026-04-11-track-b-media-experience.md`.

### v0.55.0 — A2: Export Content Options
ExportOptions type, branch filtering (BFS + spouse inclusion), living exclusion, content toggles. ExportOptionsPanel.vue. GEDCOM exporter integration. 11 unit tests.
Part of Track A: `docs/plans/2026-04-11-track-a-presentation-sharing.md`.

### v0.56.0 — C3: Place Map Visualization
Leaflet/OpenStreetMap maps: global MapView, PersonMap life path, PlaceDetailView inline map.
Part of Track C: `docs/plans/2026-04-11-track-c-core-polish.md`.

### v0.57.0 — D2: MCP Media Tools for AI
3 MCP tools (get_media_file_base64, get_untagged_media, get_media_for_person_context) + 13 unit tests.
Part of Track D: `docs/plans/2026-04-11-track-d-mcp-agent-story.md`.

### v0.58.0 — B2: Media-Bundled Portable Archive
GEDCOM + media .zip export/import with path rewriting. Uses fflate. 6 unit tests.
Part of Track B: `docs/plans/2026-04-11-track-b-media-experience.md`.

### v0.59.0 — A1: Narrative Reports
Person biography, place history, and family narrative as prose-generating Vue components. Swedish + English locale support. Deep linking from detail views. getEventsForPlace() API + 2 tests.
Part of Track A: `docs/plans/2026-04-11-track-a-presentation-sharing.md`.

### v0.60.0 — A5: CSV Export
Persons, events, sources, places as CSV with delimiter (comma/semicolon/tab) and UTF-8 BOM options. 13 tests.
Part of Track A: `docs/plans/2026-04-11-track-a-presentation-sharing.md`.

### v0.61.0 — B3: Media Timeline
Horizontal scrollable timeline with thumbnails positioned by date. Deduplication, event-linked media discovery, approximate date styling. 9 tests.
Part of Track B: `docs/plans/2026-04-11-track-b-media-experience.md`.

### v0.62.0 — A3: Wall Charts
Large-format pedigree (horizontal) and descendant (vertical) SVG charts. A4-A0 paper sizes, tiled output with crop marks. 13 tests.
Part of Track A: `docs/plans/2026-04-11-track-a-presentation-sharing.md`.

### v0.63.0 — C1: Undo/Redo
Command-pattern undo/redo for person, name, event, participant, relationship, source, citation CRUD. Keyboard shortcuts (Cmd+Z, Cmd+Shift+Z), grouped operations, toast notifications, max depth 100, clear on DB switch. 30 tests.
Part of Track C: `docs/plans/archive/2026-04-11-track-c-core-polish.md`.

### v0.64.0 — B4: Face/Region Tagging (Manual)
media_regions table with fractional coordinates, CRUD API, 5 MCP tools, lightbox drawing mode with PersonPicker. 14 tests.
Part of Track B: `docs/plans/2026-04-11-track-b-media-experience.md`.

### Docs — D3: Claude Desktop Integration
6 MCP workflow guides (research report, family history, source audit, data quality, photo tagging), README MCP setup section.
Part of Track D: `docs/plans/archive/2026-04-11-track-d-mcp-agent-story.md`.

### v0.65.0 — A4: Static HTML Site Export
Self-contained browsable family tree website: person/place/source pages, client-side search, responsive CSS, XSS-safe, living person exclusion. 12 tests. **Track A complete.**
Part of Track A: `docs/plans/archive/2026-04-11-track-a-presentation-sharing.md`.

### v0.66.0 — C4: GEDCOM Hardening
8 edge case fixtures (encoding, minimal, empty fields, deep nesting, large family, non-standard tags, malformed dates, duplicates). Hardened date parser (abt./ca/circa, ranges). Import preview dialog. 40 integration tests. **Track C complete.**
Part of Track C: `docs/plans/archive/2026-04-11-track-c-core-polish.md`.

### v0.67.0 — B5: Face/Region Tagging — MCP for AI
Batch tagging tools: suggest_media_regions, get_persons_for_matching, get_media_tagging_status. 18 tests. **Track B complete. All four tracks from the competitor gap analysis are now done.**
Part of Track B: `docs/plans/archive/2026-04-11-track-b-media-experience.md`.

---

## Implementation History (v0.68+)

See [CHANGELOG.md](../../../CHANGELOG.md) for full narrative entries. Summary table:

| Version | Feature |
|---------|---------|
| v0.68.0 | PlacePanel: map pin side panel, 8 collapsible sections, drag-resize, EntityMediaSection |
| v0.69.0 | User feedback: hierarchy section, wedding/foster event types, couple subtypes, cause field |
| v0.70.0 | Pedigree + hourglass chart outline placeholders for selected person |
| v0.71.0 | Hourglass outline architecture: TreePerson data model, unconditional outline injection |
| v0.72.0 | Pedigree and descendant charts: TreePerson data model, N-parent support, outline injection |
| v0.73.0 | Tree subject (SUBM): import matching, SUBM export, DatabaseView picker |
| v0.74.x | Fix: descendant chart outline placement and space reservation |
| v0.75.0 | Boundary gazetteer overlay — click map pin to see parish polygon |
| v0.76.0 | Bundled Swedish boundary gazetteer (Lantmäteriet, CC0) |
| v0.77.0 | Place types (municipality/locality), map boundary overlay fixes |
| v0.77.1–v0.78.4 | Hourglass layout rewrite: computeFootprint, 4-pass placement, collision avoidance |
| v0.80.0 | QualityView: confirm/reject/view buttons for place match checks |
| v0.81.0–v0.82.1 | updateMedia API, media table view, inline editing, prod/dev server split |
| v0.83.0–v0.89.1 | MCP workflow tools: persons, events, sources, places, research, media, data mgmt; dev tools |
| v0.90.0 | MCP overhaul: prod/dev split, 34 workflow tools, 15 dev tools |
| v0.91.0–v0.93.1 | UX: multi-token search, SourcePicker, DateInput, CDP debugging, quality fix actions |
| v0.94.0 | Design System Overhaul: 3 color themes, 9 UI primitives, sidebar restructure |
| v0.95.0–v0.97.6 | Detail view UX: collapsible sections, three-sheet layout, unified views |
| v0.99.x | Inline media viewer with zoom/pan, face tag drawing, map performance |
| v0.100.0–v0.102.x | Face tag region move/resize, language gazetteers (GeoNames + Wikidata) |
| v0.103.0–v0.103.3 | Shared place coordinate resolution, Life Map in PersonPanel |
| v0.104.0–v0.105.0 | Gazetteer build extraction, cross-platform build scripts |
| v0.106.0–v0.107.0 | Open source publishing infra, link rules expansion (de/da/no), fan chart |
| v0.108.0–v0.110.0 | Per-theme dark mode, wall chart generation UI, timeline chart visual overhaul |
| v0.111.0–v0.112.0 | Chart visual overhaul (WCAG contrast, curved connectors, dynamic heights), unified fan chart |
| v0.114.0–v0.116.0 | Chart box polish, pedigree/hourglass/descendant generations stepper |
| v0.117.0–v0.119.6 | Wall chart as inline report, fan chart theme-aware palettes, media lightbox removed |
| v0.120.0–v0.121.0 | Set profile from face-tag star, UI polish (notes, textarea height, outline fixes) |
| v0.122.0–v0.123.0 | PersonPicker relation hint, monospaced notes toggle |
| v0.124.0–v0.126.0 | EventForm citation section, unified zoom controls, quality checks for places/media/sources |
| v0.127.0–v0.128.1 | Add person from place panel, wall chart rolled into live charts, MCP clean shutdown |
| v0.129.0–v0.129.2 | Quality checks expansion (18 new checks), performance (bulk queries) |
| v0.130.0–v0.130.3 | Chart export controls to ReportsView, sex/bw color modes, tiled PDF fix |
| v0.131.0–v0.131.1 | Keepsake reports redesign: 7 reports + 6 prints + 6 primitives + 2 composables |
| v0.132.0–v0.132.1 | Cropped face-tag profile pictures on all AppAvatars |
| v0.133.0–v0.134.0 | Reports route persistence, media captions, PersonLifeMap, timeline chart export |
| v0.135.0 | ReportPanel: all print config controls in right-side panel, useReportConfigStore |

---

## Completed Milestones

### Hourglass Outline Architecture
Refactor hourglass chart layout to support outline placeholders as first-class nodes.
- Plan: [2026-04-11-hourglass-outline-architecture.md](2026-04-11-hourglass-outline-architecture.md)

### Hourglass Layout Rework
Complete rewrite: clone → inject outlines → measure (computeFootprint) → 4-pass placement → line routing.
- Spec: [2026-04-15-hourglass-layout-rework-design.md](2026-04-15-hourglass-layout-rework-design.md)

### Gazetteer Quality Checks + Media Editor
Gazetteer match quality checks, confirm/reject in QualityView, MediaView table mode.
- Spec: [2026-04-15-gazetteer-quality-media-editor-design.md](2026-04-15-gazetteer-quality-media-editor-design.md)

### MCP Server Overhaul
Prod/dev server split. 34 workflow tools, 15 dev tools. Factory pattern.
- Spec: [2026-04-15-mcp-overhaul-design.md](2026-04-15-mcp-overhaul-design.md)
- Plan: [2026-04-15-mcp-overhaul.md](2026-04-15-mcp-overhaul.md)

### Gazetteer IPC Refactor
Split bundled gazetteers from renderer bundle to fix Vite OOM on CI. Renderer bundle: 40 MB → 1.3 MB.
- Spec: [2026-04-20-gazetteer-ipc-refactor-design.md](2026-04-20-gazetteer-ipc-refactor-design.md)
- Plan: [2026-04-20-gazetteer-ipc-refactor.md](2026-04-20-gazetteer-ipc-refactor.md)

### Place Gazetteers
Render-time place resolution using bundled hierarchical gazetteers. 25 gazetteers, ~40 MB.
- Spec: [2026-04-11-place-gazetteers-design.md](2026-04-11-place-gazetteers-design.md)
- Plan: [2026-04-11-place-gazetteers.md](2026-04-11-place-gazetteers.md)

### Gazetteer Import/Export
Per-database gazetteer storage. 7 MCP tools for import/export/manage.
- Spec: [2026-04-13-gazetteer-import-export-design.md](2026-04-13-gazetteer-import-export-design.md)
- Plan: [2026-04-13-gazetteer-import-export.md](2026-04-13-gazetteer-import-export.md)

### Boundary Gazetteer Overlay
New "boundary" gazetteer kind carrying polygon geometry. Click map pin to see parish extent.
- Spec: [2026-04-13-boundary-gazetteer-design.md](2026-04-13-boundary-gazetteer-design.md)
- Plan: [2026-04-13-boundary-gazetteer-overlay.md](2026-04-13-boundary-gazetteer-overlay.md)

### Keepsake Reports Redesign
7 keepsake reports + 6 framable prints, 6 primitives, 2 composables, privacy filter.
- Spec: [2026-04-19-keepsake-reports-redesign-design.md](2026-04-19-keepsake-reports-redesign-design.md)
- Plan: [2026-04-19-keepsake-reports-redesign.md](2026-04-19-keepsake-reports-redesign.md)

### Media Viewer & Face Tagging
Inline image viewer with zoom/pan, face tag drawing, lightbox removed.
- Spec: [2026-04-18-media-viewer-face-tagging-design.md](2026-04-18-media-viewer-face-tagging-design.md)
- Plan: [2026-04-18-media-viewer-face-tagging.md](2026-04-18-media-viewer-face-tagging.md)

### Cropped Face-Tag Profile Pictures
Starred face tag as cropped square profile picture on every AppAvatar. Pinia store + canvas crop.
- Plan: [2026-04-20-avatar-profile-pic-crop.md](2026-04-20-avatar-profile-pic-crop.md)

### ReportPanel
All print-configuration controls moved from tab headers into a right-side panel. useReportConfigStore.
- Spec: [2026-04-21-report-panel-design.md](2026-04-21-report-panel-design.md)
- Plan: [2026-04-21-report-panel.md](2026-04-21-report-panel.md)

### Open Source Publishing
CI/CD, automated releases, Claude-powered issue triage, governance files, README redesign.
- Spec: [2026-04-18-open-source-publishing-design.md](2026-04-18-open-source-publishing-design.md)
- Plan: [2026-04-18-open-source-publishing.md](2026-04-18-open-source-publishing.md)

### IPC Channel Registry
Replaced 3-layer string-keyed IPC boilerplate with a single typed registry in `src/shared/channels/` (~131 channels). Adding a channel is now one `defineChannel()` call. `window.api` is fully typed via `ApiSurface<typeof channelRegistry>`.
- Plan: [2026-04-28-ipc-channel-registry.md](2026-04-28-ipc-channel-registry.md)

### Panel Composables & EntityPanel
`useEntityData` / `useEditableFields` / `usePanelStorage` composables and a shared `<EntityPanel>` shell. Removed ~600 lines of repetition across entity panels, fixed the EventList stale-load race, centralized 56+ ad-hoc localStorage keys, and baked cross-view reactivity into the data composable.
- Plan: [2026-04-28-panel-composables.md](2026-04-28-panel-composables.md)

### Ben Feedback Batch (2026-04-29)
25 items from beta-tester Ben shipped across 8 releases (v0.162.6 → v0.169.0). Reactivity audit + fixes (panel section counts, tree refetch on data change), life-timeline expansion (parents' deaths, opt-in sibling/child events), and assorted UX polish. See CHANGELOG entries for the full list.
- Plans: `2026-04-29-ben-*.md` in this archive directory.

### Duplicate Persons View
`/duplicates` route + `DuplicatesView.vue` listing duplicate-person candidates with a side-by-side `MergePersonsModal`. API: `findDuplicates`, `findDuplicatesPage`, `mergePersons`, `ignoreDuplicatePair`. Nav badge with live count. (Places / sources / media coverage tracked separately as a backlog item.)

### Inline Media Picker across Entity Panels
Every right-side entity panel media section (Person, Place, Relationship, Source, Group, ResearchTask) hosts the same inline `[picker | Add | Cancel]` add-row. `+ Attach` no longer jumps straight to the OS file dialog — type to autocomplete against existing media (already-linked items filtered out), or click the in-field 📎 / dropdown footer "Attach file…" to upload a new file. Three section flavors (PersonMediaSection, EntityMediaSection, LinkedMediaSection) unified on a shared `MediaAddRow` component; new `media:createFromFile` IPC keeps the wrapper agnostic of which link table the section writes to.
- Spec: [2026-05-02-inline-media-picker-design.md](2026-05-02-inline-media-picker-design.md)
- Plan: [2026-05-02-inline-media-picker.md](2026-05-02-inline-media-picker.md)

### Panel CTA Cleanup
Closes seven UX inconsistencies in the right-side panels: dead Groups row in PersonPanel (now navigates to GroupsView), raw `&#10005;` glyphs replaced with `IconUnlink` (unlink) and `IconTrash` (delete) across MediaPanel + EntityMediaSection, GroupPanel + ResearchTaskPanel unlinks now confirm via the same `useDeleteConfirm` + `<ConfirmModal>` flow PersonPanel uses, the misleading "Add relationship" header button (silently picked spouse) is gone, PlacePersonsSection's redundant duplicate row click is gone, and face-tag rows in MediaPanel show an explicit `IconPencil` reassignment affordance. Drift prevented by a new source-level scan test in `tests/components/panel-cta-conventions.test.ts` and codified in the `ux-intent-mapping` skill.
- Plan: [2026-05-02-panel-cta-cleanup.md](2026-05-02-panel-cta-cleanup.md)

### API Polymorphic Link Helpers
Collapsed ~11 near-identical `getXFor<EntityType>` queries (citations, groups, tasks, media) into `getLinkedEntities` + `getCitationsByOwner` in `src/api/links.ts`. Per-entity functions stay as 1-line wrappers so MCP/IPC callers see no diff. Shipped in v0.162.4.
- Plan: [2026-04-28-api-link-helpers.md](2026-04-28-api-link-helpers.md)

### Events Fact-Value GEDCOM Round-Trip
Separates GEDCOM-X `Fact.value` from free-form notes in the events table. New `events.value` column holds the line value of fact-shaped GEDCOM tags (`OCCU "Carpenter"`, `RELI "Lutheran"`, `EDUC "Bachelor of Arts"`, etc.); legacy `description` column renamed to `notes`. Importer now reads the line value into `value` (previously dropped silently); exporter emits it as the line value. Round-trip golden test as merge gate. EventModal shows a type-aware Value field (Yrke / Examen / Trossamfund …) + always-visible Notes textarea, with a Prime Directive guard that preserves authored data across event_type toggles. New event_types `title`/`religion`/`description`/`fact` route TITL/RELI/DSCR/FACT tags cleanly. MCP tools accept `value` + `notes`, with deprecated `description` alias for backwards compat.
- Spec: [2026-05-02-events-fact-value-design.md](2026-05-02-events-fact-value-design.md)
- Plan: [2026-05-02-events-fact-value.md](2026-05-02-events-fact-value.md)

### GEDCOM Round-Trip Fidelity Audit & Guard
Codified GEDCOM round-trip fidelity as a Prime Directive (cont.) co-equal with authored-data preservation in `CLAUDE.md`. Built `src/api/gedcom_fidelity_registry.ts` declaring round-trip status for every schema column under both 5.5.1 and 7.0 (`lossless` | `lossless-via:<mech>` | `lossy:<reason>` | `excluded:<reason>`). Three new tests enforce the contract: schema-coverage guard (adding a column without a registry entry fails CI with the column name), per-(table, column, version) round-trip (187 cases + 100 documented exclusions), and golden multi-table-seed round-trip (catches multi-field interaction regressions). Two real round-trip bugs surfaced by the per-field test were fixed inline: `formatGedcomDate` was emitting raw ISO for `BET..AND` end dates, and the repository address exporter was creating orphan `2 CITY/POST/STAE/CTRY` sub-tags when address itself was empty. The user's choice to use this app remains reversible: data they hand us comes back out, with documented `lossy` / `excluded` exceptions instead of silent loss.
- Spec: [2026-05-02-gedcom-roundtrip-fidelity-design.md](2026-05-02-gedcom-roundtrip-fidelity-design.md)
- Plan: [2026-05-02-gedcom-roundtrip-fidelity.md](2026-05-02-gedcom-roundtrip-fidelity.md)

### Place Input is a Leaf, Never a Path (MCP boundary)
RCA-driven contract tightening on the three MCP tools that write to `places.name` directly or implicitly. `add_place` and `update_place` now reject any `name` containing a comma; `record_event` does the same for `place` and additionally rejects when both `place` and the new `place_chain` are passed. Hierarchy is now expressed as an explicit array — `parent_chain` on `add_place` (root → leaf, excluding the leaf) and `place_chain` on `record_event` (root → leaf, including the leaf) — both routed through the existing `findOrCreatePlaceWithChain` so missing rows are created and existing ones reused (idempotent). Tool descriptions and `docs/MCP.md` rewritten to teach the convention. Closes the "Chennai, India, World, India, World" bug where an agent's path-shaped string had been persisted verbatim. Importers and the renderer's `PlacePanel.onNamePlaceSelected` smell are explicit scope deviations (importers preserve the source file; the renderer follow-up has its own stub plan).
- Plan: [2026-05-03-place-leaf-only-mcp.md](2026-05-03-place-leaf-only-mcp.md)

### Place Name PlacePicker Leaf-Only (renderer) — obsoleted before written
Stub follow-up to the MCP-side leaf-only fix, written as a placeholder for closing the renderer's `PlacePanel.onNamePlaceSelected` smell that wrote a comma-string path into `places.name`. Obsoleted before being expanded into a real plan: commit `7b040b17` ("fix: unify place modal/panel form + drop silent picker-merge overwrite") removed the entire handler — the Place panel's name field is now a plain text input with no PlacePicker-merge affordance, so there is no surface left to fix. The "merge-from vs pivot-to" decision was answered by deletion. Filed for the historical record so the next person grepping for `onNamePlaceSelected` finds the trail.
- Plan: [2026-05-03-place-name-picker-leaf-only.md](2026-05-03-place-name-picker-leaf-only.md)

### Hourglass Chart Polish (2026-05-04)
Six fixes from Bengt's R45–R50 / R54.1 batch shipped in v0.212.0. Siblings and shared children now render oldest-leftmost; partner edges per-pair so multi-partner relationships don't cross intermediate spouse boxes; shared children hang from the couple connector midpoint instead of one parent; foster parent–child edges render dashed (`8 4` stroke pattern) with an SVG `<title>` hover tooltip; clicking a relative pans the chart to keep them on screen with a 100 px inset short-circuit. R50 dual-focus regression-tested but no active bug found in current code (`chartNavFocusedPerson` is wired only to PedigreeChart).
- Spec: [2026-05-04-hourglass-chart-polish-design.md](2026-05-04-hourglass-chart-polish-design.md)
- Plan: [2026-05-04-hourglass-chart-polish.md](2026-05-04-hourglass-chart-polish.md)

### Global Gazetteer Hierarchy
Migrated the entire gazetteer system from a forest of 30 self-rooted trees (`Sverige × 6`, `Danmark × 3`, `World (Historical)`, etc.) into one canonical hierarchy rooted at `World` with `World (Historical)` as a sibling super-root. Every per-country and per-source gazetteer (29 of 30 — `lang-sv-wikidata` keeps its existing translation data; the other two language gazetteers re-keyed to canonical paths) emits a self-rooted tree typed by the closed admin vocabulary `world | continent | country | admin{N}`. The structural-merge engine (`src/api/place-gazetteers/merge.ts`) collapses same-`(name, type, parent_path)` nodes across sources by aliases-union + first-wins coords; `__contributors: string[]` tracks per-node provenance. End-to-end resolver verified across SE/FI/NO/DK/IS/US/CA/DE plus historical empires. Imported user gazetteers must root at `World` or `World (Historical)`. `GazetteerNode.type` tightened to the closed vocab; closed-vocab integrity test enforced. Multiple design iterations during execution (contract over fixture; no leaf-type vocabulary; no cross-source merging on leaves but structural agreement permitted at admin levels; clean source → clean script → clean gazetteer → mechanical join). All 30 build scripts re-run with fresh source data fetched 2026-05-03.
- Spec: [2026-05-03-global-gazetteer-hierarchy-design.md](2026-05-03-global-gazetteer-hierarchy-design.md)
- Plan: [2026-05-03-global-gazetteer-hierarchy.md](2026-05-03-global-gazetteer-hierarchy.md)

### NewPersonModal Hardening
Save in "Lägg till ny person" is now disabled until the user has typed at least one name field — replacing a post-save toast warning that fired only after the malformed row had already been written. The change cascades through every server-side person-creation path: a new throw in `createPerson` (with an opt-in `{ allowNameless: true }` for importers preserving source-file reference graphs), MCP-handler-level guards in `create_person` and `add_child`, and explicit warning entries in import reports for GEDCOM/Holger/Genney/archive paths when an INDI/PERSON record carries no NAME tag. Existing nameless rows in user databases are surfaced via a new `PERSON_NO_NAME` quality check that detects both zero-`person_names`-rows and all-blank-fields cases (replacing the narrower `NO_NAME` zero-rows-only check). Audit covered 17 person-write paths; 8 risk paths closed via two root-cause fixes (api guard + Genney inline guard). 228 test fixtures bulk-migrated to the explicit `allowNameless: true` opt-in.
- Spec: [2026-05-04-new-person-dialog-hardening-design.md](2026-05-04-new-person-dialog-hardening-design.md)
- Plan: [2026-05-04-new-person-dialog-hardening.md](2026-05-04-new-person-dialog-hardening.md)

### Panel Table Column Overflow — Clip + Ellipsis (2026-05-04)
Long text in any `.panel-section .data-table` cell (events, names, citations, group memberships, research tasks, quality issues) was breaking character-by-character into a vertical strip when the column was narrower than the string. Cause: shared rule `.panel-section .data-table td { word-break: break-word; overflow-wrap: anywhere }` outweighed every per-cell `white-space: nowrap` override on specificity. Replaced the wrap rule with a clip pattern (`white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 0`); small fixed-content cells (badges, dates, sex symbol, ✕ trash button) opt back in with `width: 1px; max-width: none; white-space: nowrap`. Long-text cells gained `:title="<full value>"` for hover-reveal of clipped text. Touched 12 panel-table components (EventList, LinkedPersonsSection, LinkedPlacesSection, LinkedMediaSection, PlacePersonsSection, PersonMediaSection, EntityMediaSection, RelationshipsList, GroupsTable, ResearchTasksTable, QualityIssuesTable, SourcePanel inline citations). Component test asserts computed style matches the user-observable contract. R43 (inline add-new from picker) dropped per user direction.
- Spec: [2026-05-04-person-picker-ux-design.md](2026-05-04-person-picker-ux-design.md)
- Plan: [2026-05-04-person-picker-ux.md](2026-05-04-person-picker-ux.md)

### Event Participants Parity + Marriage-Flow Prompts
Closed the asymmetry where editing a wedding/marriage/engagement/divorce hid the "Other person" picker that's visible at create time — the affordance is now symmetric across both states (Part A.1, `EventModal-edit-second-person.test.ts`). Added a `<EventParticipantsSection>` component that's rendered unconditionally for every entry in `EVENT_TYPE_VALUES`, so witnesses on a wedding, godparents on a baptism, and mourners on a funeral can be added through a single Deltagare / Participants section using the existing `eventParticipants.add/getForEvent/remove` IPC surface — no new channels (Part A.2, deferred Roles UI per Part B). On RelationshipModal save success, a `couple+marriage` relationship with no linked wedding event surfaces a non-blocking `ConfirmModal` offering to record the wedding inline; Yes opens EventModal as a `mode="subpanel"` flow pre-filled with `default-event-type='marriage'` and `relationship_id`; No writes nothing (Part C, Prime Directive guard with explicit test). On RelationshipModal save *before* persisting a new couple, a read-only `findUnresolvedPartnership(person1Id)` walks existing couple relationships, filters out those with a linked divorce event or a deceased other partner, and surfaces a non-blocking warning when an overlap is detected; the user can proceed or cancel (Part D). Divorce-mirror offer deferred — `CoupleSubtype` has no `divorced` value, divorces are tracked as separate event rows linked via `relationship_id` with no relationship-modal trigger; documented in code comment for the future when the data model gains the concept.
- Spec: [2026-05-04-event-participants-and-marriage-flow-design.md](2026-05-04-event-participants-and-marriage-flow-design.md)
- Plan: [2026-05-04-event-participants-and-marriage-flow.md](2026-05-04-event-participants-and-marriage-flow.md)

### PersonPanel Relations Ordering (2026-05-04)
Open the same person twice and see the same Relationer list in the same order, every time. Replaced the panel's insertion-order render path with a pure sort function (`src/api/sortPersonRelations.ts`) shared between `PersonRelationshipsSection.vue`, `ALifeReport.vue`, and `LifeOnOnePageReport.vue`. Order: parents (bio → adopted → foster → step → unknown, M → F within each, sex-typed headings for bio/unknown, foster-terminology's sex-neutral role tokens for adopted/foster/step) → partners chronological by `start_date` (null sinks last by id) → children inlined under producing partner (oldest-first by birth_date) → orphan-children bucket → other relations (godparent + sibling first, then social, both `Intl.Collator(locale)`-sorted for å/ä/ö correctness in Swedish). Renderer collapsed to a single `reports.personSummary` IPC per panel open (down from 1 + 5×N). Rebased onto foster-terminology when it landed mid-execution; non-bio parent headings now route through `relationshipLabels.ts`. Component test "produces identical DOM order when remounted" mechanically asserts the user goal.
- Spec: [2026-05-04-person-relations-ordering-design.md](2026-05-04-person-relations-ordering-design.md)
- Plan: [2026-05-04-person-relations-ordering.md](2026-05-04-person-relations-ordering.md)

### Beta Tester UX Batch (2026-05-05) — Reports 56–68
Seven UX fixes from beta tester reports against v0.215.2: per-row partner heading is singular ("Partner" not "Partners"); date_original field gains a label + helper text and a quality check flagging digit-free rows so existing misuse can be cleaned up without auto-clearing authored data; Förnamn sort uses preferred name (tilltalsnamn) when marked so sort matches displayed name; list table headers are sticky across every list view via a single `shared.css` rule (per-view duplications removed); side-panel section headers gain a subtle background band on `var(--surface-hover)` for low-vision scannability; chart controls (zoom, generation count, fan arc, color mode) carry hover tooltips on every icon-only / number-only button; new persons created from any "+ Ny person" entry point can capture birth date + place inline in the same modal step via the existing `persons.createWithEvent` workflow. Each plan is self-contained per `.claude/rules/plans.md`.
- Plans: [2026-05-05-partner-singular-heading.md](2026-05-05-partner-singular-heading.md), [2026-05-05-date-original-field-clarity.md](2026-05-05-date-original-field-clarity.md), [2026-05-05-given-name-sort-uses-preferred.md](2026-05-05-given-name-sort-uses-preferred.md), [2026-05-05-list-sticky-headers.md](2026-05-05-list-sticky-headers.md), [2026-05-05-personpanel-section-header-color.md](2026-05-05-personpanel-section-header-color.md), [2026-05-05-chart-controls-tooltips.md](2026-05-05-chart-controls-tooltips.md), [2026-05-05-add-parent-with-birth-event.md](2026-05-05-add-parent-with-birth-event.md)

### Beta Tester UX Batch (2026-05-06) — Reports 69–80 (partial)
Six fixes/features from beta tester reports against v0.215.2: place picker preserves typed text after a pick so Backspace at the trailing edge edits one character at a time instead of clearing the whole field (root cause: `modelValue` watcher's null branch wiped `query` unconditionally — predicate guard now compares against `lastResolvedPath`); tall modal forms scroll inside the modal so Save stays reachable on small windows (root cause: `.entity-panel`'s `min-height: min-content` silently outweighed its `max-height: calc(100vh - 64px)` cap per CSS spec — single-line fix to `min-height: 0` plus 6-case CSS-contract test); place-tree picker icon and tooltip explain what the panel shows (hierarchy-glyph SVG, Swedish/English tooltip "Browse places already in this database", info-pill panel header distinguishing DB rows from gazetteer suggestions); relation row uses clearer role labels (full word "Förälder" instead of truncated "Fö" — root cause: `.panel-section .data-table td { max-width: 0 }` truncation, lifted via scoped `td.type-cell` override) plus hover tooltips on every affordance and a trash icon for remove; research tasks can be linked to persons via a new `PersonResearchTasksSection` self-loading component on PersonPanel and a `+ Task` CTA that auto-links the host person (Surface contract Check #1, mechanically asserted by `ResearchTaskModal-host-link.test.ts`); source types curated for Swedish genealogy with five new entries (passenger list, probate inventory, peerage register, encyclopedia, genealogist's work) plus relabels for newspaper/online_database, dropdown sorted alphabetically in the user's locale via `Intl.Collator(locale)` over rendered i18n labels (round-trip via existing `_STYPE` GEDCOM custom tag, no schema change). Plus two unrelated fixes on main: `display_id` index moved out of inline CREATE TABLE block so pre-v0.218 databases no longer crash on app launch; dev MCP tools (`db_stats`, `seed_*`, `clear_test_data`) now follow `switch_database` swaps instead of staying pinned to the closed initial connection.
- Plans: [2026-05-06-place-picker-edit-after-pick.md](2026-05-06-place-picker-edit-after-pick.md), [2026-05-06-modal-scrollable-content.md](2026-05-06-modal-scrollable-content.md), [2026-05-06-place-tree-picker-discoverability.md](2026-05-06-place-tree-picker-discoverability.md), [2026-05-06-relations-row-affordances.md](2026-05-06-relations-row-affordances.md), [2026-05-06-research-tasks-link-persons.md](2026-05-06-research-tasks-link-persons.md), [2026-05-06-source-types-curation.md](2026-05-06-source-types-curation.md). Held for follow-up: relationship-modal-broken (R78 — REPRO confirmed: Save click silently fails with generic toast "Could not save. Please try again."; modal stays open; Save button stays active-looking; root cause not yet identified — needs renderer-side devtools console inspection); hourglass-foster-vs-adoptive-distinct (R79 — REPRO confirmed: foster and adopted edges currently render with identical `stroke-dasharray="8 4"`, indistinguishable to user); name-citation-and-validity (R75 — WIP on plan/name-citation-and-validity branch); sex-change-guard (R80).

### Hourglass Foster Edge + Couple Edge (2026-05-06) — superseded
Authored 2026-05-06 to address two reported hourglass-chart symptoms (R66/67/77): foster edge flips to solid when a second foster parent is added; couple connector between focal and partner disappears when partner becomes a co-parent. Live MCP-driven reproduction in bengt.db on v0.221.1 confirmed **neither symptom reproduces**: foster edge stays dashed (the chart correctly merges both parents' edges into a single curved path from couple-midpoint to child); couple edge Z↔P remains solid and visible. Per the plan's own self-supersession clause ("If either symptom doesn't reproduce on current main, it may have been fixed incidentally by Fix 1"), the plan is archived without code changes. The 2026-05-05 dedup change (Fix 1) appears to have closed both edge-rendering symptoms incidentally.
- Plan: [2026-05-06-hourglass-foster-edge-and-couple-edge.md](2026-05-06-hourglass-foster-edge-and-couple-edge.md)

### Beta Tester UX Batch (2026-05-06) — Reports 69–80 (final wave)
Four follow-up plans completing the 2026-05-06 beta-tester batch on top of the partial wave already archived above. **Relationship-modal-broken** (R78): live repro confirmed Save was failing silently with a generic toast; fix bound `:save-disabled` to a `canSave` computed (person1+person2+type+distinct-ids) so the button is visibly dimmed when fields are missing AND the catch-block toast now appends the rejected promise's message ("`Could not save. Please try again. — FOREIGN KEY constraint failed`") so the user sees WHY save failed. The "red row" was identified as the entity-coloured Events section header, not an error state — UX label question stays open as a separate decision. **Hourglass-foster-vs-adoptive-distinct** (R79): live repro confirmed foster + adopted edges rendered identically (both `stroke-dasharray="8 4"`); fix introduces `dashForSubtype` (foster `8 4`, adopted `2 3`, biological none, step deferred to foster) PLUS a mixed-subtype split — when coupled parents have different subtypes the chart now splits the merged curved-path into two per-parent edges so each subtype's dash is visible. Same-subtype merge preserved. Adoptive-relationship legend entry wired alongside the previously-orphaned foster entry. **Name-citation-and-validity** (R75): completed from a stalled WIP commit. Schema migration adds `citations.person_name_id` (nullable FK with CASCADE on name delete; FK_VIA_XREF round-trip, GEDCOM 7 emits SOUR under NAME, v5.5.1 falls back). PersonNameModal grows a Hänvisning section that buffers pending citations on the create path and flushes after `persons.addName`; `date_to` ('Giltigt till') is hidden for `name_change` and `birth`, relabelled per name type ('Used until' for `alias`/`aka`); save handler emits `date_to` unconditionally so legacy values aren't nulled out (Prime Directive guard). **Sex-change-guard Phase 1** (R80): the original cross-stack plan stalled at the 10-min watchdog when dispatched as one piece; split into three sub-plans, with Phase 1 shipping the data layer — new `gender_transition` event type, an `updatePerson` guard that throws `SexChangeRequiresConfirmationError` (carrying `personId` + `activeRelationshipIds`) when sex changes on a person with active relationships and neither `confirmCorrection` nor `confirmGenderTransition` is set, an atomic `updatePersonWithGenderTransitionWorkflow` (BEGIN IMMEDIATE), and a pure render-time `resolveParentSexAt` resolver that walks `gender_transition` events to compute sex-as-of-date. Phase 2 (modals) and Phase 3 (MCP + GEDCOM) drafted after Phase 1 lands. Plus three smaller fixes on main during the batch: `display_id` index moved out of inline CREATE TABLE block (pre-v0.218 DB crash); dev-MCP tools follow `switch_database` swaps (was pinned to closed initial connection); `db_stats` switched off the sql.js `getAsObject` API to node-sqlite3-wasm's `queryOne`, and `app_status` reports the live DB path instead of the launch-time env var.
- Plans: [2026-05-06-relationship-modal-broken.md](2026-05-06-relationship-modal-broken.md), [2026-05-06-hourglass-foster-vs-adoptive-distinct.md](2026-05-06-hourglass-foster-vs-adoptive-distinct.md), [2026-05-06-name-citation-and-validity.md](2026-05-06-name-citation-and-validity.md), [2026-05-06-sex-change-guard-phase1-data-layer.md](2026-05-06-sex-change-guard-phase1-data-layer.md). Held for follow-up: sex-change-guard Phase 2 (UI modals) + Phase 3 (MCP + GEDCOM round-trip) — to be drafted on top of Phase 1's API surface.

### Long-running IPC off the main thread (2026-05-06)
A user-reported 25-second freeze during a 22 k-person Holger import traced to `phaseIndividuals` running synchronously on the Electron main thread. Audit found 11 importer/exporter `wrapHandler` blocks all using the same shape: dialog → `getDb()` → heavy DB walk → result. Migrated each one to the DB worker thread per the channel-registry pattern in `src/shared/channels/`. Six channels with no inline dialog (`import:holgerRun`, `gedcom:import`, `gedcom:preview`, `import:genneyRun`, `import:genneyDiscover`, `website:previewSnapshot`) moved directly to `defineChannel({ thread: 'worker' })`. Four channels with inline dialogs (`archive:import`, `archive:export`, `gedcom:export`, `csv:export`) keep their public name on main as a thin shim that opens the dialog then calls a `:_`-prefixed internal worker channel for the heavy work — the `:_` prefix is exempted from preload/static-api coverage so internal channels are never exposed to the renderer. Two website channels (`buildPreviewHtml`, `export`) stay on main with documented scope deviation: they need Electron's `nativeImage` + `dialog`, and their heavy DB walks already delegate to existing worker channels via `callWorker('website:buildSnapshot', …)`. New `withImportLifecycle('<name>', fn)` helper consolidates the `setWorkerImportInProgress(true/false)` flag flip and timing logs across all four importers; introduced ahead of Tasks 4/5 to prevent copy-paste drift. Worker→main→all-windows `broadcast(topic, payload)` primitive added so importer `onProgress` callbacks can reach the renderer from the worker thread (replaces the old main-side `webContents.send`). Renderer adapted in three places to consume the new `{ success, report, error }` envelope shape (`ArchiveSection.vue`, `GedcomImportSection.vue`, `GenneyImportSection.vue`). Architectural invariant pinned by `tests/unit/main-thread-responsive-during-import.test.ts` — moving any of the 10 listed heavy channels back to `thread: 'main'` fails CI.
- Spec: [2026-05-06-long-running-ipc-off-main-design.md](2026-05-06-long-running-ipc-off-main-design.md)
- Plan: [2026-05-06-long-running-ipc-off-main.md](2026-05-06-long-running-ipc-off-main.md)

### Compress shipped JSON assets (2026-05-08)
Two independent tracks under one version bump (0.226.0). Track A (gazetteers): the 29 bundled gazetteer JSONs in `src/api/place-gazetteers/data/` ship as `.json.gz` sidecars at `<bundle-dir>/gazetteers/<id>.json.gz` instead of raw JSON. The Vite `compress-bundled-gazetteers` plugin gzips each file (level 9) in `closeBundle`; `bundled.ts` loads them at module init via `gunzipSync(readFileSync(...))`, resolving the path against `import.meta.url` and falling back to source `data/<id>.json` for tests/dev. The packaged asar drops from ~62 MB of JSON to 9.7 MB total — installer ships ~46 MB smaller. Worker config no longer needs its own gazetteer plugin (no static `.json` imports remain in source). Track B (website export): `website:export` IPC handler gains a `mode: 'split' | 'portable'` parameter. Split (default) writes `index.html` + sibling `data.json.gz`, fetched + decompressed by the static SPA bootstrap via `DecompressionStream` — minimal hosted-deployment shape. Portable writes a single self-contained `index.html` with the snapshot embedded as base64-gzip in an inline `<script>window.__SNAPSHOT_GZ__='...'</script>`, decompressed in-page so the file works from `file://` (email/USB/double-click). Static SPA `installStaticApi` extends to a 4-path bootstrap (legacy `__SNAPSHOT__`, embedded gz+base64, fetched gz, dev raw `data.json`). `WebsitePanel.vue` adds a Format section with two radios; choice persists to localStorage. E2E covers both modes' file shape + snapshot round-trip.
- Plan: [2026-05-08-compress-shipped-json-assets.md](2026-05-08-compress-shipped-json-assets.md)

### Third-Party Licenses + SBOM (2026-05-09)
Bundle a generated `THIRD_PARTY_LICENSES.txt` into the packaged app and surface it from Settings → About → "View open source notices" (`LicensesViewerModal.vue`, new `app:readThirdPartyLicenses` IPC). The script (`scripts/build-third-party-licenses.mjs`) walks `npm ls --omit=dev --all` plus `npm ls electron`, sorts deterministically, and throws on any package without a recognizable LICENSE file (no silent skip, per `feedback_no_silent_string_replace.md`). Forge `generateAssets` hook regenerates the file on every package run; `extraResource` lands it at `<resourcesPath>/THIRD_PARTY_LICENSES.txt`. The same file plus a CycloneDX `sbom.cdx.json` (full dep tree — Electron is a devDependency that physically ships, so `--omit=dev` would mislead) attach to every GitHub Release as standalone supply-chain artifacts. Tightens `window.api.app` typing in `src/renderer/api.d.ts` from `LooseFallback`'s `Promise<unknown>` to explicit signatures.
- Plan: [2026-05-09-third-party-licenses-and-sbom.md](2026-05-09-third-party-licenses-and-sbom.md)

### Bundle Size + Memory Reduction (2026-05-09)
Three independent workstreams under one version bump (0.235.0) targeting the question "does the user download a smaller installer and see the same app?". Track A (asar): `forge.config.ts` `packagerConfig.ignore` switched from a regex denylist to a function-based allowlist that keeps only `/.vite/**` and `/package.json` inside the asar. The Vite plugin already bundles every non-`external` JS dep, so anything else (src/, node_modules/, docs, dev configs) was dead weight; macOS app.asar drops from 128 MB to 10 MB. Track B (gazetteers): the 36 bundled gazetteer JSONs are encoded to a packed binary format (`.glb.gz`) at build time via a new `src/gazetteer-build/binary-codec.ts` (deduplicated string table, int32 lat/lon, delta-encoded geometry). The Vite `emit-bundled-gazetteers-binary` plugin replaces the `compress-bundled-gazetteers` gzip-of-JSON plugin; `bundled.ts` decodes via `decodeGazetteer(gunzipSync(readFileSync(...)))` with `.json.gz` (transitional) and `data/*.json` (dev/test) as fallbacks. Contributors still author JSON in `data/` per `feedback_gazetteers_are_build_outputs.md`. Bundled gazetteer payload drops from 7.30 MB to 5.64 MB. Track C (statement cache): a 5-file audit found and finalized 20 leaked SQLite prepared statements (3 in `media_consolidate.ts` BEGIN/COMMIT/ROLLBACK calls; 17 in `genney/transform.ts` per-import — every Genney import previously leaked all 17 to the WASM heap). Also added `rollup-plugin-visualizer` (gated on `VISUALIZE=1`) for future renderer-bundle audits. Lazy + LRU + pre-warm gazetteer loading deferred to a follow-up plan if the 5.64 MB payload still warrants it. WASM heap smoke test (live packaged-binary GEDCOM import) deferred to user verification.
- Plan: [2026-05-09-bundle-and-memory-reduction.md](2026-05-09-bundle-and-memory-reduction.md)

### First-Time Onboarding (2026-05-09)
Bengt's beta-tester corpus exposed five distinct first-time confusion patterns: chart focus-switch undiscoverable (#25), media-storage convention invisible (#13b), face-tag UI unintelligible, media reorder hidden, panel sections that show a blank rectangle instead of explaining what they're for. The plan ships two mechanisms under one user goal ("the genealogist understands every panel section without asking"): (1) Empty-state coaching across every panel section in PersonPanel, PlacePanel, SourcePanel, GroupPanel, ResearchTaskPanel, MediaPanel — `SectionEmpty` grew a `purposeKey` mode that pulls Purpose copy from `docs/UX_INVENTORY.md` (one i18n key per surface, sv + en in lockstep). EventParticipantsSection migrated alongside; ReportPanel and WebsitePanel are configuration forms and are explicitly N/A-marked. Coverage is mechanically guarded by `panel-empty-state-coverage.test.ts` so any future panel section without coaching breaks CI. (2) Four enumerated `Coachmark` instances anchored to real DOM elements: Hourglass focus (auto-dismiss on first dblclick), Media reorder (gated on ≥2 rows), Face-tag drawing (only while drawMode active), and a one-shot toast on first media attach explaining the `<dbname>-media/` copy. State persists per-installation in `settings.json.onboarding.seen` (NOT per-DB — Bengt's "tested in tiny DB then opened real DB" pattern would re-fire every coachmark otherwise); reachable from Settings → Defaults → "Reset onboarding". Architecture: `useFirstEncounter(key)` composable + 3 IPC channels (getSeen / markSeen / reset) registered via `wrapHandler` (main-thread-only because `settings.ts` imports electron) with manual `window.api.onboarding` typing in `api.d.ts`; static SPA gets a no-op stub so read-only website exports never trigger coaching.
- Spec: [2026-05-09-onboarding-design.md](2026-05-09-onboarding-design.md)
- Plan: [2026-05-09-onboarding.md](2026-05-09-onboarding.md)

### European Gazetteer Roadmap — Tier 1 + Tier 2 (2026-05-09)
End-to-end European place-resolution coverage from a 14-plan roadmap. Bundled gazetteer count 29 → 71 across versions 0.229.0 → 0.239.0. **Tier 1 (10/10):** DE upgrade brought Germany to parity with the Nordics — added BKG vg250 boundaries (16 Bundesländer + 400 Kreise polygons, 5.4 MB raw / 0.34 MB gzip) and Wikidata Kirchengemeinden + Pfarreien (Q20820021 + Q17143723 + Q102496 — 61 parishes with admin1 chain across 8 of 16 Bundesländer; sparse first cut). GB shipped admin1+admin2+boundaries from ONS Open Geography Portal (4 home nations + 361 LADs); IE/NL/BE/FR/EE/LV/LT/PL each shipped admin1+admin2+populated-places from GeoNames country dumps (CC BY 4.0). **Tier 2 (3 batched plans / 31 countries):** Western (AT/CH/IT/ES/PT/MT/SM/LI/AD/MC), Central (CZ/SK/HU/SI/HR/BA/RS/ME/MK/AL/XK/LU), Eastern (BG/RO/MD/GR/CY/BY/UA/FO/GL). FO and GL ship as standalone gazetteers (not folded into dk-*). Critical lesson surfaced mid-DE: the originally-drafted Wikidata QIDs were unvalidated and several were grossly wrong (Q1620908 = "historical region" not Kirchengemeinde; Q73501 = "Bredevoort" the Dutch town; Q1860233 = "Pentland Firth" a Scottish sound; Q3308141 = "Michael Delisle" a Canadian writer; Q23498 = "archaeology"). Mandatory `wbgetentities` validation gate (design § 3.2) added to every Wikidata-sourced plan in Task 0; corrections propagated across IE/NL/BE/EE/LV/LT/PL/europe-historical plans. Closed-vocab discovery: parish leaves use `admin3` per `GAZETTEER_NODE_TYPES` in code, not `parish` as the skill doc claimed (sv-socknar / dk-sogne precedent). Per-gazetteer `stripSuffixes` / `stripPrefixes` rules ship per country (kraj/okres, megye/járás, voivodeship/powiat, opština + Cyrillic forms, …). The registry-driven `tests/unit/european-coverage.test.ts` carries country-specific resolution probes with sequence-order assertion that prevents wrong-reason passes (DE plan caught this when the boundary gazetteer changed admin2 names from `Kreisfreie Stadt Lübeck` to bare `Lübeck`). **1 plan remaining**: `europe-historical` (Wikidata SPARQL extension to `world-historical` covering Habsburg crown lands, Russian gubernii, German Bund states, Polish partitions, Soviet republics — paused mid-session due to Wikidata socket-drop reliability + 4 TBD QIDs needing `wbsearchentities` discovery). **Per-country follow-ups deferred:** parishes for the 9 non-DE Tier 1 countries, ru-eu / tr-eu (longitude-filtered admin1+2), boundaries for IE/NL/BE/FR/EE/LV/LT/PL.
- Design: [2026-05-09-european-gazetteers-design.md](2026-05-09-european-gazetteers-design.md)
- Plans: [2026-05-09-de-gazetteer-upgrade.md](2026-05-09-de-gazetteer-upgrade.md), [2026-05-09-gb-gazetteer.md](2026-05-09-gb-gazetteer.md), [2026-05-09-ie-gazetteer.md](2026-05-09-ie-gazetteer.md), [2026-05-09-nl-gazetteer.md](2026-05-09-nl-gazetteer.md), [2026-05-09-be-gazetteer.md](2026-05-09-be-gazetteer.md), [2026-05-09-fr-gazetteer.md](2026-05-09-fr-gazetteer.md), [2026-05-09-ee-gazetteer.md](2026-05-09-ee-gazetteer.md), [2026-05-09-lv-gazetteer.md](2026-05-09-lv-gazetteer.md), [2026-05-09-lt-gazetteer.md](2026-05-09-lt-gazetteer.md), [2026-05-09-pl-gazetteer.md](2026-05-09-pl-gazetteer.md), [2026-05-09-tier2-western-gazetteers.md](2026-05-09-tier2-western-gazetteers.md), [2026-05-09-tier2-central-gazetteers.md](2026-05-09-tier2-central-gazetteers.md), [2026-05-09-tier2-eastern-gazetteers.md](2026-05-09-tier2-eastern-gazetteers.md)

### Bernadotte MCP Test Session (2026-05-09)
End-to-end agent-driven test: build the full Swedish royal Bernadotte line (Karl XIV Johan → Carl XVI Gustaf → Estelle/Oscar) plus the Norwegian royal house with a cross-family link via Crown Princess Märtha and an explicit adoption (Marius Borg Høiby → Haakon Magnus, `subtype: 'adopted'`) — 60 persons, 101 relationships, 131 events, 62 places, 90 media with 50+ face tags, 6 research tasks. The session was structured as a discovery test for the agent-facing MCP surface; the resulting docs catalogue 19 distinct gaps that the test surfaced. **Eighteen fixes + one verified-clean** landed in the same session: data:changed broadcast wiring (worker → preload, regression-tested in `tests/unit/data-changed-broadcast.test.ts`); tolerant date parser that kills 232 of 359 false positives in `run_checks` (`parseLooseDate` / `extractYear` / `dateDefinitelyAfter` rewritten on top of robust ISO + free-text + Swedish/English month-name parsing); `add_place` leafProps drop on the parent_chain branch; `createSource` INSERT silently dropping `abstract` + `call_number`; `merge_persons` post-merge dedupe (single-cardinality events with citation transfer + `name_type='birth'` → `aka` demotion); empty `default_person_id` → first-person-by-surname render-time fallback in PersonsView; place_type vocabulary expansion (palace / castle / church) + MCP enum sync with renderer constant; i18n `eventTypes.accession` + `coronation` (and the EVENT_TYPE_VALUES entries that were missing alongside); `v-show` instead of `v-if` for sections that derive their count via `defineExpose({count})` (Forskning + Kvalitet on PersonPanel + PlacePanel + MediaPanel); resizable columns with localStorage persistence (`useResizableColumns` composable + `.table-resizable` CSS variant + `.col-resize-handle` drag affordance, applied first to `QualityIssuesTable.vue`); the table-layout-fixed-vs-width-100% gotcha that made Issue column resize silently squashed; new `PersonIdentifiersSection.vue` (FamilySearch / Ancestry / Riksarkivet / personnummer / GEDCOM REFN/RIN / other) wired into PersonPanel between Namn and Händelser; `ui_reload` dev-MCP tool (Cmd+R via the bridge); `ui_get_dom` extended with `mode` (outerHTML / innerHTML / textContent / attributes), `all`, `limit` for targeted DOM extraction; renamed `mcp-dev` skill → `slaktforskning-mcp-dev` for namespace consistency with the new sibling `slaktforskning-mcp` skill (agent USING the MCP for genealogy research); 14-row reload matrix in `slaktforskning-mcp-dev` mapping file path to reload strategy; new "Verifying interactive UI fixes via MCP" subsection acknowledging the bridge can't synthesize drag interactions; three new pitfall sections (pass-through-in-branches, `mutating: true` matters, v-show vs v-if for `defineExpose({count})`); two napkin entries (parallel-merge `git mv + edit = D + A` shape, MCP-can't-drag); two project-wide UI rules in `renderer.md` (resizable-columns adoption pattern, never combine `table-layout: fixed` with `width: 100%`). One open finding remains (#9 — `living: true` heuristic for >130-yr-old persons without death event; by-design, low priority).
- Findings: [2026-05-09-bernadotte-test-findings.md](2026-05-09-bernadotte-test-findings.md)
- Followups: [2026-05-09-bernadotte-followups.md](2026-05-09-bernadotte-followups.md)

### Beta-tester May 7 reports (86–93) batch (2026-05-09)
Five plans answering reports 86–93 from the beta-tester corpus, shipped together. **Timeline kin-event labelling** turned the person timeline into a relationship-aware narrative — every kin event now reads as a relationship ("Sons födelse", "Förälders död", "Vigsel — partner"); foster children's biological birth events no longer appear on a foster parent's timeline (the foster placement event surfaces instead when dated); every dated row, including kin rows, gets the focal person's age in a dedicated visual column to the right of the spine. Render-time only — no schema change. The composer (`composeTimelineLabel` in `src/renderer/utils/timelineLabel.ts`) is the single source of labelling truth and also drives `ALifeReport`. **Persons list aggregate columns** turned the list into a sparseness scanner: opt-in columns for sex + 7 count columns (names, events, relationships, media, groups, tasks, quality issues), persisted via a `⋮ Kolumner` picker; secondary sort lifted into `usePagedList` (shift-click any header) with a status pill; default tiebreaker is name ASC. Aggregates ship as a single SQL query per page with new FK indexes. **Phase 4 (cross-view secondary sort on the other 5 list views) was dropped from scope** mid-execution as a misapplied all-or-nothing rule — no driving user feedback existed, and shipping shift-click without underlying API support would have been the exact "silent degradation" failure mode the renderer rules forbid. **Event outside-lifespan check** is a new informational quality check covering both before-birth and after-death; save-time non-blocking warning toast in `EventModal` plus a row in the Quality view (chronology category). Covers all participant roles, not just primary. Never modifies the saved event. **PersonNameModal hardening** ports the new-person-dialog hardening pattern to the name-event modal: prefill given_name (previously only surname was prefilled), red asterisks + "Obligatoriskt" helper lines on conditionally-required fields, Save button greyed and `aria-disabled` while invalid, Enter through invalid form flashes the offending field instead of silently doing nothing. Mononyms still allowed. **Notes monospace toggle label** added a visible `Fast teckenbredd` / `Monospace` text label alongside the `iWi` mnemonic (now `aria-hidden`) on all three Notes-hosting surfaces — addresses a low-vision beta-tester confusion. Rich-text formatting on Notes was deferred to a future brainstorm (storage format, GEDCOM round-trip implications, editor library, sanitisation, search are all open questions). All five plans verified by diff inspection + unit/component tests; live smoke walkthrough remains user-deferred.
- Plans: [2026-05-09-timeline-kin-event-labelling.md](2026-05-09-timeline-kin-event-labelling.md), [2026-05-09-persons-list-aggregate-columns.md](2026-05-09-persons-list-aggregate-columns.md), [2026-05-09-date-before-birth-quality-check.md](2026-05-09-date-before-birth-quality-check.md), [2026-05-09-person-name-modal-hardening.md](2026-05-09-person-name-modal-hardening.md), [2026-05-09-notes-monospace-toggle-label.md](2026-05-09-notes-monospace-toggle-label.md)

### GEDCOM lossless: events.place_address via _PLAC_ADDR (2026-05-09)
Promoted `events.place_address` from `lossy` to `lossless-via:_PLAC_ADDR` for both GEDCOM 5.5.1 and 7.0. Exporter emits the custom sub-tag at level 3 under PLAC, or at level 2 directly under the event when no PLAC line is emitted (so authored addresses survive even on event records without a place). Importer reads it from either position. Distinct from the standard ADDR/CITY/POST handling which targets the Place's mailing-address columns. Per-field fidelity harness now exercises the round-trip mechanically. — [plan](2026-05-09-gedcom-event-place-address.md)

### GEDCOM lossless: sources.abstract + sources.call_number via _ABSTRACT / _CALL (2026-05-09)
Promoted `sources.abstract` and `sources.call_number` from `lossy` to `lossless-via:_ABSTRACT` / `lossless-via:_CALL` for both GEDCOM 5.5.1 and 7.0. Exporter emits each as a custom level-1 sub-tag under SOUR; multi-line abstracts are split across CONT continuation lines on export and rejoined by the parser on import, so embedded newlines round-trip byte-identical. Distinct from `REPO.CALN` which carries the repository's own call-number on a different table. The matching `repositories.call_number` column is a related but separate follow-up (different exporter phase, different carrier under REPO). — [plan](2026-05-09-gedcom-source-abstract-call-number.md)

### GEDCOM lossless: relationships.notes via _RELA_NOTE on ASSO for non-couple types (2026-05-09)
Closed the non-couple branch of `relationships.notes` so notes on sibling, godparent, and "other" relationships round-trip end-to-end. Couple notes already rode `_RELNOTES` on FAM (a custom level-1 sub-tag under FAM); ASSO had no standard NOTE child the importer reads back, so non-couple notes were silently dropped on export. Exporter now emits `2 _RELA_NOTE <notes>` (with `3 CONT <line>` continuation for multi-line notes) under each ASSO block; importer reads it back when creating the deduplicated relationship row. Both endpoints' ASSO blocks carry the note (the exporter writes the relationship under each person); the importer's existing dedup ensures only one DB row results. Registry: kept the entry as `lossy` (per the precedent set by `relationships.subtype`) but updated `expectedAfterRoundTrip` so couple/sibling/godparent/other return the seeded value; only `parent_child` still returns `''` — `parent_child` rides FAMC/FAMS in the GEDCOM model with no current NOTE carrier on those structures, tracked as a follow-up. — [plan](2026-05-09-gedcom-relationship-notes.md)

### GEDCOM lossless (v7.0 only): citations.transcription via _TRANS on SOUR for person/family/place hosts (2026-05-09)
Promoted `citations.transcription` to `lossless-via:_TRANS` under GEDCOM 7.0 — the user goal is that a parish-record transcription pasted on any citation (event, name, person, relationship, place) survives export and re-import. Event/name already rode standard DATA/TEXT under SOUR (lossless on both versions); the missing branches were person-level (SOUR on INDI), family-level (SOUR on FAM, mapped to relationship_id) and place-level (SOUR on the custom `_PLAC` top-level record). Exporter emits `2 _TRANS <transcription>` under SOUR for those three host kinds with `3 CONT` continuation for multi-line transcriptions so embedded newlines round-trip byte-identical. Importer reads `_TRANS` from each non-event/name citation phase. **Option A on the double-emit question:** `_TRANS` is emitted ONLY for person/relationship/place hosts under v7.0 — never alongside DATA/TEXT for event/name hosts and never under v5.5.1 — keeping the file minimal and removing any "which one wins on import" ambiguity. **Registry split by version:** this is the first plan in the batch where the entry is split — v7.0 is `lossless-via:_TRANS`, v5.5.1 stays `lossy` because 5.5.1 is historically stricter about unknown sub-tags inside SOUR cites; promoting v5.5.1 would be a separate plan focused on consumer-tolerance testing against a panel of 5.5.1-consuming apps. — [plan](2026-05-09-gedcom-citation-transcription-v70.md)

### GEDCOM lossless: groups + group_links via custom _GROUP / _GROUP_LINK records (2026-05-10)
Promoted every non-audit `groups.*` and `group_links.*` column from `lossy → null` to `lossless-via:_GROUP` (or `_GROUP_LINK`) for both GEDCOM 5.5.1 and 7.0. Each `groups` row exports as a level-0 `_GROUP` top-level record carrying NAME and NOTE (multi-line via CONT continuation); each `group_links` row exports as a `1 _GROUP_LINK` sub-record with `2 TYPE person|place|media` and `2 REF @xref@` resolving back to the host. Polymorphic xref resolution: persons → existing INDI map; places → the existing top-level `_PLAC` phase, extended to emit records for places that are group-linked (in addition to places with citations) so the xref dereferences; media → a new top-level OBJE phase that emits records for media linked to a group (inline OBJE blocks under INDI/FAM/event carry no xref so they can't be referenced from a level-1 sub-record). New importer phase `phaseGroupRecords` runs after persons/places/media phases (so all xref maps are populated), creates the `groups` row, and walks each `_GROUP_LINK` resolving REF against the right map; refs that don't dereference (dangling xref) get an explicit `ImportReport.warnings` entry rather than being silently dropped — closes the disclosure loop on offboarding round-trips. `group_links.sort_order` stays `lossy → 0` matching `person_names.sort_order` (visible membership order is preserved by emit position; the column itself rebases per-(group, entity_type) on import). The previously-emitted "Groups and group membership" entry in `ExportReport.excluded` is removed — the data is no longer dropped. Per-field fidelity harness now exercises the round-trip for every promoted column; golden seed extended with two groups carrying mixed-type members (3 persons + 1 place + 2 media in g1; 1 person + 1 place in g2) to catch multi-field interaction regressions. — [plan](2026-05-09-gedcom-groups-custom-tag.md)

### Duplicates Panel — Places, Sources, Media (2026-05-10)
Extended the persons-only `/duplicates` view to cover places, sources, and media in a four-tab shell. Each tab uses an entity-specific find-duplicates heuristic (Levenshtein ≤ 2 within an author/parent_place_id grouping for places/sources; same `file_ref` or near-equal title for media) and an entity-specific merge function that snapshots every touched row pre-merge for undo. `mergeMedia` additionally takes `keepFile: 'target' | 'source'` so the function never silently deletes a file the user wanted to keep. New MCP tools `merge_places`, `merge_sources`, `merge_media`; the existing `find_duplicates` MCP tool gains an optional `entity` argument (default `'person'`). `DUPLICATE_*` quality-check rows now deep-link via `?tab=<entity>&pair=<id1>:<id2>` into the right tab with the pair pre-opened. Polymorphic `ignored_duplicates` table generalized via a v0.220.0 migration that drops the FK to `persons` and adds `entity_type DEFAULT 'person'` (preserving every existing person-typed ignore row). Three new merge modals copy the persons-modal compare layout rather than factoring a shared shell — three near-duplicates is the cleaner choice given the divergent field shapes.
- Design: [2026-05-09-duplicates-panel-design.md](2026-05-09-duplicates-panel-design.md)
- Plan: [2026-05-09-duplicates-panel.md](2026-05-09-duplicates-panel.md)

### Tauri Port — full migration from Electron 41 (2026-05-11)
Replaced Electron 41 with Tauri 2.x. The Vue 3 renderer is unchanged from the user's perspective; what flipped is the runtime + IPC + SQLite backing. SQLite is rusqlite via Tauri commands; the db-worker thread is retired (rusqlite is itself off-thread via `spawn_blocking`). `window.api` is auto-wired at boot in `src/renderer/tauri-window-api.ts` by walking the channel registry, with explicit polyfills for ~30 main-only channels (dialogs, file IO, native shell). MCP runs as a Tauri sidecar process — no `npx tsx` on PATH required. Auto-update is the Tauri 2.x updater plugin; binaries are signed (notarized on macOS, Authenticode on Windows, GPG'd AppImage on Linux). Plan-shape: a spike (evaluation) → "go" verdict → full-port plan → completion-audit (the gap-list) → completion-plan (cluster dispatches) → test-migration plan. Goal-anchor verification: the Holger reference user round-tripped a real OurKind GEDCOM through the Tauri UI, and Windows was smoked end-to-end. Auto-updater hop test (0.252.0 → 0.252.1) is deferred to a follow-up release. The Electron entry points (`src/main/**`, `src/preload/**`, db-worker, electron-forge config) are retained in source for the duration of the cross-over window — they ship to neither Tauri nor Electron after this release.
- Spike: [2026-05-09-tauri-port-evaluation.md](2026-05-09-tauri-port-evaluation.md) — produced the "go" recommendation in `tauri-port-evaluation-recommendation.md` (in the evaluation worktree)
- Plan: [2026-05-10-tauri-full-port.md](2026-05-10-tauri-full-port.md) + execution notes [2026-05-10-tauri-full-port-notes.md](2026-05-10-tauri-full-port-notes.md)
- Gap audit: [2026-05-10-tauri-port-completion-audit.md](2026-05-10-tauri-port-completion-audit.md)
- Gap-closing plan: [2026-05-10-tauri-port-completion-plan.md](2026-05-10-tauri-port-completion-plan.md)
- Test migration: [2026-05-10-tauri-test-migration.md](2026-05-10-tauri-test-migration.md)

### ARIA dev MCP tools — TTS-parity surface (2026-05-12)
Added seven `ui_aria_*` tools to the dev MCP that mirror what a screen-reader user experiences. Started as a half-day v1 plan (`ui_aria_list` + `ui_aria_invoke` only) — shipped via subagent + parent live-smoke on day one — then expanded mid-execution to the full TTS-parity surface after the user observed that the half-measure was just "screen-reader-inspired CSS selectors". v2 adds: `ui_aria_tab_order` (sequential focus walk), `ui_aria_landmarks` (every landmark with `has_name` field), `ui_aria_headings` (H-key navigation), `ui_aria_read` (DOM-order reading-units a TTS would announce), `ui_aria_audit` (six finding-kinds with severity ranking and per-kind hints). Element state (`pressed` / `expanded` / `selected` / `checked` incl. `'mixed'` / `current` / `busy` / `invalid` / `required`) surfaces on every result. Goal-anchor verification: live smoke against Settings → Länkregler used only `ui_aria_*` tools (zero CSS selectors in the tool log), and `ui_aria_audit()` surfaced 27 real a11y gaps in one call — Settings chip strip lacking `role="tab"`, modal inputs without label associations, 12 unnamed row-checkboxes, unnamed `<main>`. Three production-only bugs caught at smoke time (esbuild `__name` helper undefined, duplicate `runScript` export, region-flooding from unnamed `<main>`) seeded a new test class — serialization round-trip — that evaluates `buildAria*Script` outputs and compares to direct resolver calls. App-level a11y findings tracked separately in `docs/plans/2026-05-12-app-a11y-gaps.md` as a new `[planned]` entry. — [plan](2026-05-11-aria-mcp-tools.md)

### Skipped-tests cleanup — convert 112 registry-excluded skips to passing assertions (2026-05-12)
The pre-plan `npm test` summary read *"3996 passed | 112 skipped"* and all 112 skipped lines were structural noise — `tests/unit/gedcom-fidelity-per-field.test.ts`'s generator emitted one `it.skip(...)` per `(table, column, gedcom-version)` triple where the column was documented in `gedcom_fidelity_registry.ts` as `excluded` (audit timestamps, UUIDs, derived `normalized_name`, etc.). User goal: *"the skipped count in the summary is a number I can trust — if it says zero, no tests are disabled."* Converted the generator's `excluded`-branch from `it.skip(reason, () => {})` to `it(reason, () => { expect(status.kind).toBe('excluded'); expect(status.reason.length).toBeGreaterThan(0); })` — same per-cell coverage with the registry's reason still in the test name, but reported as passing. Added standing-guard test `tests/unit/no-skipped-tests.test.ts` that statically scans `tests/**/*.ts` for `it.skip(` / `test.skip(` / `describe.skip(` / `it.todo(` / `test.todo(` and fails CI if any return; conditional `.skipIf(...)` / `.runIf(...)` (legitimate fixture-presence guards used in `gramps-transform.test.ts` and `rootsmagic-real-sample.test.ts` for gitignored sample files) are explicitly allowed. Post-plan summary: `Tests 4109 passed (4109)` with 0 skipped. Addresses gap #17 from the Tauri full-port RCA. — [plan](2026-05-12-skipped-tests-cleanup.md)

### Gazetteer lazy chunks — drop the 8 GB Node-heap workaround (2026-05-12)
Switched the renderer-aliased `src/renderer/empty-gazetteers.ts` from `import.meta.glob('../api/place-gazetteers/data/*.json', { eager: true, import: 'default' })` (which inlined every JSON into a single ~30 MB chunk and OOM'd Vite's rollup pass on the default 2 GB heap) to `import.meta.glob(..., { query: '?url' })` — every gazetteer JSON ships under `dist-tauri/assets/<id>-<hash>.json` and the webview fetches + parses each one on demand. The previously-30 MB `tauri-window-api-*.js` chunk is now 504 KB. Removed the `cross-env NODE_OPTIONS=--max-old-space-size=8192` workaround from `start` / `dev` / `build` / `build:e2e` in `package.json` and dropped `cross-env` from `devDependencies`. The renderer-side gazetteer surface (`getAllGazetteers` / `getGazetteerById`) is now async; the Node-side `bundled.ts` (used by tests + the MCP sidecar) stays synchronous, with a new `getBundledGazetteerIds()` mirror so api/ files that need just the ID list compile in both runtimes. **Mid-execution course-correct:** the first-attempted shape (non-eager `{ import: 'default' }`, one code-split chunk per JSON) ALSO OOMed the build — Vite still parses every JSON to emit each chunk. The `query: '?url'` shape is the only one that short-circuits the JSON-as-module pipeline. New regression test `tests/unit/empty-gazetteers-no-eager.test.ts` asserts the glob declares `query: '?url'` so nobody silently re-introduces JSON-as-module loading. — [plan](2026-05-12-gazetteer-lazy-chunks.md)

### Vite 5.4.21 → 7.3.3 — match the Tauri 2.x default toolchain (2026-05-12)
Lifted the entire Vite-family toolchain to the version `npm create tauri-app@latest` ships today: `vite` 5.4.21 → 7.3.3, `@vitejs/plugin-vue` 5.2.4 → 6.0.6, `vitest` + `@vitest/coverage-v8` 4.1.x → 4.1.6 (Vitest 4 supports Vite 7 — no Vitest 5 needed). `vite-plugin-node-polyfills` (0.26.0), `vite-plugin-singlefile` (2.3.3), and `rollup-plugin-visualizer` (7.0.1) already declared Vite 7 in their peer deps so no version change was required for those — Task 5's plugin-replacement fallback was not exercised. Every `vite build` log no longer opens with "The CJS build of Vite's Node API is deprecated" because Vite 7 ships ESM-only; `package.json` adds `"type": "module"` so node loads `.js` / `.mjs` configs as ESM by default, and `vite.static.config.ts` derives `__dirname` via `fileURLToPath(import.meta.url)` to keep `outDir` resolving from the config's own directory under module semantics. No user-facing behavior change — renderer bundle output, dev server port, lazy-gazetteer chunk emission (71 JSON assets present in the post-bump build), and `dist-static/index.html` shape were verified unchanged after the bump (4111 unit tests green; `npx vite build --config vite.renderer.config.ts` and `vite build --config vite.static.config.ts` both exit 0 with no CJS warning anywhere in the output). — [plan](2026-05-12-vite-7-upgrade.md)

### E2E test repair — `[smoke]` → `[boot]`, fix two real Tauri-port leftovers (2026-05-12)
Repaired the Playwright suite. Pre-plan, the suite ran 6 tests across 4 projects with 2 failing: `[smoke]/app.test.ts` ("Unexpected end of JSON input" parsing the `/eval` response) and `[duplicates]/duplicates.spec.ts` (a stack-only WebKit error in 1.5 s). The plan's hypothesis (gazetteer-init burst pinning the renderer past `executeJs`'s timeout) was wrong on both counts — the gazetteer-lazy-chunks plan that landed first removed the suspected cause AND neither failure was timing-related. Real causes: (a) `app.test.ts` was still hitting the Electron-era `/execute_js` endpoint and parsing the response as `{ result: boolean }`, missed during the Tauri port's test migration — Tauri serves `/eval` and returns the raw value or `{ __error: "..." }`; (b) `window.api.duplicates.mergeMedia` was undefined in the Tauri build because the channel registry intentionally skips `mergeMedia` (it does sync `fs` work that's banned in worker handlers per `.claude/rules/api.md`) and the Tauri port had no compensating polyfill. Fixed in `src/renderer/tauri-window-api.ts` by adding an explicit `api.duplicates.mergeMedia` polyfill that resolves `dbPath` via `db_current_path` and calls `api/duplicates.ts` directly; allowlist updated in `tests/unit/tauri-channel-coverage.test.ts`. Renamed the Playwright `[smoke]` project to `[boot]` per L3 from the Tauri-port RCA — every comment / `startApp(..., 'smoke')` tag updated; `grep -rn smoke tests/e2e playwright.config.ts` now returns zero. CLAUDE.md "Finishing a plan" step 0 grew an explicit `npx playwright test → 4 passed (Xs)` evidence line item alongside `npm test` and `npm run build`. Post-plan run: `6 passed (11.6s)` with all four projects (`[boot]`, `[crud]`, `[website-export]`, `[duplicates]`) green. — [plan](2026-05-12-e2e-test-repair.md)

### App-level a11y gaps surfaced by `ui_aria_audit` (2026-05-12)
Cleanup pass against the 27 findings the dev MCP's `ui_aria_audit` tool produced on Settings → Länkregler in a single call. Settings tab strip, the chart-kind tab strip on `/persons`, and the Import/Export tab strip refactored to the ARIA tab pattern (`role="tablist"` + `role="tab"` + `aria-selected` + `aria-controls` + roving `tabindex` + `role="tabpanel"` + `aria-labelledby`). `<main>` carries an `aria-label` derived from the active route's i18n title (longest-prefix match handles `/persons/:id` etc.) so `D`/`VO+U` jumps to it by name. Modal form inputs across CitationModal, EventModal, GroupModal, LinkRuleModal, PersonModal, PersonNameModal, PlaceModal, RelationshipModal, ResearchTaskModal, SourceModal converted from `<span class="ep-field-label">` + `<input>` to `<label for=>` + `<input id=>` programmatic associations. Aktiva regler row-checkboxes and PlacePicker / PersonPicker inputs gained per-instance `aria-label`. The MCP audit resolver also gained spec-correct support for wrapping `<label>` ancestors as programmatic associations. New CI test `tests/components/aria-audit-zero-findings.test.ts` mounts each covered surface in happy-dom and runs the same audit logic the dev MCP runs against the live app, asserting findings is either empty or a per-surface allow-list (which starts empty); `tests/components/FilterChips-tablist.test.ts` pins the tablist contract. Task 6 (live audit walk across every other route + each modal not yet on the SURFACES map) is documented for the parent session in the plan's "Tasks discovered during execution" with a concrete checklist. — [plan](2026-05-12-app-a11y-gaps.md)

### Crate / identifier rename — `tauri-spike` → `slaktforskning` (2026-05-12)
Retired the proof-of-concept naming the Tauri implementation shipped under in 0.252.0. `src-tauri/Cargo.toml`'s `[package].name` and `[lib].name` are now `slaktforskning` / `slaktforskning_lib`; `src-tauri/tauri.conf.json`'s bundle `identifier` is `com.slaktforskning.app`; the inner Mach-O binary, the e2e fixture's binary-lookup, the MCP probe handshake string in `src-tauri/src/mcp.rs`, and the `scripts/mcp-tauri.mjs` fallback DB path follow. The fallback path now reads the `identifier` field out of `tauri.conf.json` (via a tiny `readIdentifier()` helper) so the next rename is a one-place edit. The CI workflow's cache keys + artifact names were already cleaned up by prior work; the SKILL.md doc references were updated to `com.slaktforskning.app` and the legacy `mv` instruction lives in the CHANGELOG entry where developers will look for it. Active docs (`docs/plans/2026-05-12-tauri-port-rca.md`, `docs/plans/2026-05-12-audit-recommendation.md`) had their historical references rewritten in the past tense ("the proof-of-concept crate name") so the user-goal verification grep — `git grep -n "tauri.spike\|tauri_spike" -- ':!docs/plans/archive' ':!CHANGELOG.md' ':!.claude/worktrees'` — returns zero. Heads-up: legacy dev data under `~/Library/Application Support/com.slaktforskning.tauri-spike/` (the only audience is the developer who imported during shakedown) needs a manual `mv` to the new path; CHANGELOG entry has the macOS / Linux / Windows one-liners. — [plan](2026-05-12-rename-tauri-spike.md)

### Vite 7.3.3 → 8.0.12 + MCP sidecar Node 20 → 22 (2026-05-13)
Lifted the toolchain one major above the Tauri 2.x default. Vite 8's headline change is swapping the underlying build pipeline from Rollup/esbuild to Rolldown/Oxc behind a compatibility layer; the renderer build now produces the same chunk shape (71 separate gazetteer JSON assets + 1 inlined under `assetsInlineLimit: 4096` for the 2.4 KB `mc-quartiers.json`, largest chunk 5.7 MB — no eager-collapse) in 839 ms instead of multi-second Rollup runs. `vite.renderer.config.ts`'s `minify: 'esbuild'` became a deprecated alias that auto-routes to the Oxc Minifier (no edit required); `vite-plugin-singlefile` produced an identical 1.4 MB / 416 KB-gzip single-file static SPA. The single underlying intent that joined the two halves: every Node runtime in the project sits on Node 22 — host CI was already on 22, only the `@yao-pkg/pkg`-fabricated MCP sidecar binaries were still on the embedded Node 20. Bumped to `node22-*` pkg targets in `scripts/build-mcp-sidecar.mjs` (4 sites in `ALL_TARGETS`, 3 in `hostPkgTarget()`, 1 esbuild `--target=node22`); `@yao-pkg/pkg-fetch` ships prebuilt node22 binaries for all four target triples, no new dep. Companion peer-dep alignment (no version change): `@vitejs/plugin-vue` 6.0.6, `vite-plugin-node-polyfills` 0.26.0, `vite-plugin-singlefile` 2.3.3, `vitest` 4.1.6 already declared `vite ^8`. Sibling commit on the same branch repaired a pre-existing macOS e2e fixture rot — `tests/e2e/fixture.ts` hardcoded the bundle name `Släktforskning (Tauri).app` dropped in ca50d226, then `build:e2e` switched to `--no-bundle` in 390d3fc0; the fixture now falls back to the raw `target/release/slaktforskning` binary (matching the Linux fallback). Verification: `npm test` 4119/4119; renderer + static builds clean; `npx playwright test` 6 passed across `[boot]`, `[crud]`, `[website-export]`, `[duplicates]` — the `[boot] MCP server starts and responds` test exercises the `node22-*` sidecar under Tauri's spawn env. RCA on the plan itself: peer-dep ranges are NOT a substitute for reading the official migration guide; this plan was first written as a "cosmetic minor bump" before fetching `vite.dev/guide/migration`, then rewritten when the Rolldown/Oxc swap surfaced. Rule encoded in the plan's Failure-modes footer for future toolchain bumps. — [plan](2026-05-13-vite-8-upgrade.md)

### kkrpc + Bun sidecar migration (2026-05-14, archived same day)
Roadmap §1.4 item. Replaced the `@yao-pkg/pkg`-built per-platform `node22-*` sidecar binaries with a shipped Bun runtime (`src-tauri/binaries/bun-<triple>`) spawning the esbuild ESM-bundled MCP server (`dist-mcp/server.bundle.mjs`) via `tauri-plugin-shell::sidecar`. Build pipeline collapsed from two tools (esbuild + pkg, ~150 LOC orchestration) to one (esbuild only, 50 LOC). New `scripts/fetch-bun-binaries.mjs` downloads Bun 1.3.14 binaries SHA-verified against `src-tauri/binaries/bun-binaries.lock`; Tauri's `beforeDevCommand` / `beforeBuildCommand` invoke it before the renderer build. `src-tauri/src/mcp.rs::probe_mcp_sidecar` rewrote spawn from `Command::new("mcp-server-<triple>")` to `app.shell().sidecar("bun").args([resolve_bundle_path(app)])`. tauri.conf.json: `externalBin` → `binaries/bun`; `bundle.resources` added `../dist-mcp/server.bundle.mjs` + `../dist-mcp/data/*` (gazetteer JSONs). Capability `shell:allow-execute` block updated: `name: "bun", sidecar: true, args: true` (the Rust side passes an absolute bundle path from `app.path().resource_dir()`, so fixed-args wouldn't match). `release.yml` CI matrix runs `node scripts/fetch-bun-binaries.mjs` before `tauri build`. **kkrpc Rust crate skipped — protocol-incompatible with MCP JSON-RPC 2.0** (kkrpc has its own wire envelope, uses sync I/O, doesn't integrate with tauri-plugin-shell). kkrpc lives only as a JS-side dep; Rust spawns Bun directly via tauri-plugin-shell and reads/writes MCP JSON-RPC stdio frames. `@yao-pkg/pkg` + `@yao-pkg/pkg-fetch` removed from package.json. New compatibility gate test `tests/unit/bun-sidecar-spawn.test.ts` spawns the bundle under Bun and verifies the MCP `initialize` handshake succeeds — 1/1 passes. macOS signing: APPLE_SIGNING_IDENTITY already wired in release.yml; tauri-action handles externalBin signing — no explicit codesign step needed. JS-side TypeScript 4.5 → 5.9 bumped to match kkrpc's peer range (companion commit). One subagent dispatch failed mid-task on a "Not logged in" auth error; manual close-out resolved verification + CHANGELOG. — [design](2026-05-14-kkrpc-sidecar-design.md) — [plan](2026-05-14-kkrpc-sidecar.md)

### Specta migration — renderer↔Rust IPC typesafety (2026-05-14, archived same day)
Roadmap §1.3 item. Replaced the hand-rolled `src/shared/channels/` registry (1,720 LOC, 21 files) with [Tauri Specta](https://github.com/oscartbeaumont/tauri-specta) v2 codegen. 47 `#[tauri::command]` functions (audit undercounted at 29) plus every type used in command signatures carry `#[specta::specta]` / `derive(specta::Type)`. `tauri_specta::Builder::commands(collect_commands![...])` + `.export()` generates `src/renderer/bindings.ts` (308 LOC) at compile time via a `cargo test export_specta_bindings`. Runtime: `.invoke_handler(builder.invoke_handler())` replaces `tauri::generate_handler![]` — Specta is the live IPC dispatcher. `serde_json::Value` couldn't be derived directly (Specta v2-rc-25 stack-overflows on Value's recursive type) — solved via a `wire::JsonValueWire` newtype with `#[serde(transparent)]` + hand-rolled empty `Type` impl. Renderer's `tauri-window-api.ts` `mountWindowApi()` inlines all renderer-local bindings into `mutating()` / `readOnly()` helpers (was a registry walk pre-rewrite) and delegates Rust-backed commands to `commands.*` from `bindings.ts` via an `unwrap()` helper that strips Specta's `{status: 'ok'|'error'}` envelope. Deletions: `src/shared/channels/` (entire directory), 23 coverage tests (`channels-*.test.ts`, `*-worker-channel.test.ts`, `tauri-channel-coverage.test.ts`), `api-type.test-d.ts`; `static-api-coverage.test.ts` slimmed to legacy-channel-only checks; `.claude/agents/ipc-mcp-wirer.md` retired. Documentation: `CLAUDE.md` File Map points at `src/renderer/bindings.ts`; `tauri-bridge` + `add-feature` skills updated. **Type-driven coupling check requires `vue-tsc`** (not bare `tsc` — project pins TS 4.5.5); documented in CLAUDE.md. `vue-tsc` demo: rename a Specta-exported binding's param type → tsc flags the renderer call site (`error TS2345`). 4097/4097 tests pass post-deletion. Plan 3.6 (`tauri-window-api.ts` per-domain split) is now obsolete — its contingency premise (Specta descoped) didn't materialize; archived without execution. — [design](2026-05-14-specta-migration-design.md) — [plan](2026-05-14-specta-migration.md)

### Tauri-window-api per-domain split — contingency, never executed (2026-05-14, archived same day)
Roadmap §3.6 contingency. Would have decomposed the 1,302-LOC `tauri-window-api.ts` into per-domain files under `src/renderer/api/` IF plan 1.3 (Specta migration) was descoped. 1.3 shipped, replacing the entire hand-rolled IPC layer; this plan is moot. Archived for historical reference only. — [design](2026-05-14-tauri-window-api-split-design.md) — [plan](2026-05-14-tauri-window-api-split.md)

### Perf baseline capture, Rust-side (2026-05-14, archived same day)
Roadmap §1.2 item. First dispatch returned BLOCKED with diagnostics: Tauri on macOS uses WKWebView, not Chromium DevTools — Safari Web Inspector is the renderer-side debugger but isn't subagent-executable (interactive GUI). Plan revised to pivot Rust-first via `samply` (CLI tool, subagent-executable). Captured three workloads against `linda.db` (833 persons, 272 places, 3008 events, 4 media — closest stress fixture on disk; no DB on hand meets the original ≥1k places / ≥5k persons threshold without seed_family-from-running-app). Files committed to `docs/baseline-perf/2026-05-14/`: `boot-rust.json` (7.6s wall-clock; top self-time dominated by `dyld4::PrebuiltLoader::loadDependents` 1093ms), `place-resolve-rust.json` (1.2s wall-clock; mostly captures vitest startup not the resolver — `dyld4::APIs::dlopen` 1941ms), `dedup-rust.json` (29.7s wall-clock; samply's own `findClosestSymbol` symbolication 16s ⚠️ — measurement artifact flagged in summary.md). `summary.md` documents methodology + how-to-read + reference list. **Renderer-side traces NOT captured** — deferred to user-driven Safari Web Inspector follow-up per plan revision. **Honest assessment**: these baselines are diagnostic infrastructure more than tight workload measurements; future Tier 3 refactors compare against these specific numbers BUT must also document if they re-record without `--unstable-presymbolicate` (which produced the dedup self-time artifact). `.claude/skills/performance-profiling/SKILL.md` updated with "Baseline Storage Convention" pointer. — [plan](2026-05-14-perf-baseline-capture.md)

### Chart-layout property-assertion library (2026-05-14, archived same day — audit premise was wrong)
Roadmap §3.2 item. The 2026-05-14 audit's premise was: "chartLayout.test.ts is 1,663 LOC of golden snapshots; replace them with property assertions." Investigation discovered the test file has **zero** `toMatchSnapshot` / `toMatchInlineSnapshot` / `expect(layout).toEqual(...)` calls — all 165 `expect()` calls were already property-style (`toBeCloseTo`, `toBeLessThan`, etc.). There were no goldens to delete. **This is the third audit-premise correction in the 2026-05-14 batch** (3.5 modal-template-vs-script-setup, 3.7 panel-section-vs-danger-zone, 3.2 already-property-style). Plan still shipped value: built `tests/unit/chart-layout/properties.ts` with 7 named property-assertion helpers (`assertNoOverlaps`, `assertParentDirection`, `assertGenerationAlignment`, `assertOutlineAdjacency`, `assertCoupleSpacing`, `assertConnectivity`, `assertStableExtent`) as forward-looking infrastructure for future chart-layout work; wired them in alongside existing fixture tests (123 → 130 tests); built `tests/unit/chart-layout/regression-fixtures.ts` with three deliberately-broken layouts that verify the property assertions fire with named messages ("Box A at (100,100,80×40) overlaps Box B at (110,110,80×40)"). Library deviates from plan skeleton because the project's actual types (`ChartLayout`, `BoxLayout`, `LineLayout`) differ from the plan's fictional `Layout`/`Box`/`Line`: `Box.id` is `box.person.id`; `Line` is geometric (`{x1,y1,x2,y2}`), not edge ids; boxes don't carry `generation` field — so `assertParentDirection` takes an explicit `edges` arg and `assertGenerationAlignment` takes an explicit `generations` array. Phase 2 (refactor `hourglass.ts` internally) was never the real blocker; if pursued, it's a separate plan with its own audit. 130/130 chart-layout tests pass. — [design](2026-05-14-chart-layout-property-tests-design.md) — [plan](2026-05-14-chart-layout-property-tests.md)

### Modal composable extraction (2026-05-14, archived same day — user goal met, LOC targets aspirational)
Roadmap §3.5 item. Audit's original "extract event-type-specific field components" premise was corrected during brainstorming — re-measurement showed EventModal was 105 LOC `<template>` + **721 LOC `<script setup>`**; the bottleneck was composition logic, not template branching. Plan pivoted to composable extraction along the canonical state/validation/save axes (same shape as the project's `useEntityData`, `useEditableFields`). Shipped 14 composables for the four largest modals: EventModal gets 5 (`useEventForm`, `useEventValidation`, `useEventCitations`, `useEventParticipants`, `useEventSave`); PersonNameModal/PersonModal/RelationshipModal get 3 each (form + validation + save). Each composable has its own unit test under `tests/unit/composables/` (57/57 pass). Modal LOC after migration: EventModal 1052 → 1042, PersonNameModal 701 → 630, PersonModal 646 → 521, RelationshipModal 568 → 504 (total -270 LOC). **User goal met** — state/validation/save logic now lives in dedicated composables; each modal imports ≥3 of them. **LOC targets NOT met** (plan said EventModal ≤400, others ≤300) — root cause: substantial per-modal domain orchestration that wasn't in the 5-composable scope (baptism companion + name-change companion + spouse picker in EventModal; per-name citation flow in PersonNameModal that uses a distinct IPC channel; entity-creation template branches + add-related orchestration in PersonModal; wedding-offer + overlap-check flows in RelationshipModal). Pattern: each modal carries ~150-400 LOC of pre/post-save orchestration that the design didn't enumerate. **Follow-up opportunity** (deliberately not in 3.5's scope): extract `useCompanionBaptism`, `useSpousePicker`, `useCitationsForPersonName` (generalize the existing `useEventCitations`), `useWeddingOffer`, `useOverlapCheck` — each ≥2 modal usage could collapse further LOC. Open a new roadmap item if pursued. — [design](2026-05-14-modal-composable-extraction-design.md) — [plan](2026-05-14-modal-composable-extraction.md)

### report_data split (2026-05-14, archived same day)
Roadmap §3.4 item. Decomposed `src/api/report_data.ts` (1,208-LOC monolith) into 7 per-report files + shared types + helpers under `src/api/report_data/`. Reports: `person-summary.ts` (121), `family-unit.ts` (85), `ancestor-tree.ts` (76), `place-history.ts` (72), `research-gaps.ts` (60), `timeline.ts` (473), `alive-in-year.ts` (267). `types.ts` (42) holds genuinely-shared interfaces (`EventWithPlace`, `CitationWithSource`, `RelationshipSummary`). `shared.ts` (97) holds discovered-after-migration helpers: `resolveEventPlace` (6 files), `resolveEventsPlaces` (5 files), `getPrimaryName` (2 files), `getBirthSurnameForDisplay` (2 files), `findEventByType` (2 files) — extracted only because they're identical-shape across ≥2 consumers. Rejected (single-use): `resolveCitationSource`, `buildFamilyMember`, `extractYearMonthDay`, `eventDateOnOrBefore`, `eventDateOnOrAfter`, `plusMonths`, `readSubjectLifetime`, `familyEventWithinLifetime`. Rejected (cousin pattern): alive-in-year's `pickDisplayedName` / `pickBirthSurname` (operate on bulk SQL row shape with date-aware tiebreaker; structurally different from per-person `PersonName[]` walks). Monolith deleted; `index.ts` (27) re-exports the public report builders. 53/53 report-data tests pass. — [design](2026-05-14-report-data-split-design.md) — [plan](2026-05-14-report-data-split.md)

### GEDCOM phases split (2026-05-14, archived same day)
Roadmap §3.3 item. Decomposed `src/import/gedcom/phases.ts` (1,163-LOC monolith with 14 sequential phase functions) into per-phase sibling files under `src/import/gedcom/phases/`. Each phase function moved with the imports it actually needs; `phases.ts` deleted; `phases/index.ts` is the new entry point that re-exports all 14 phases + the two shared tag constants (`PERSON_EVENT_TAGS`, `FAMILY_EVENT_TAGS` in `shared.ts`). Largest sub-file: `individuals.ts` at 415 LOC (with its `KNOWN_INDI_TAGS` gating set colocated, ↑ from plan's ~385 estimate); `families.ts` 193 LOC (`KNOWN_FAM_TAGS` colocated). All other phase files under 100 LOC. `import-core.ts`'s `from './phases'` resolves to `./phases/index.ts` via Node module resolution — orchestration call sequence unchanged. 126/126 GEDCOM tests pass at every step. Inter-phase context leakage via `ImportContext` (all phases peek at all maps) was explicitly preserved per the plan's deviations — narrowing context per phase is a future plan. Execution rolled out in three waves: small phases first (notes, repo, groups, todos, obje) to validate the pattern, then 5 medium phases (asso, place-citations, prep-places, prep-inline-media, sources), then the final 4 (submitters, group-records, families, individuals) + index + monolith delete. One agent stalled mid-Task-15 (phaseFamilies, ~173 LOC); recovered cleanly by re-dispatching with the same context. No factory pattern, no class hierarchy — flat sibling files per the design's anti-abstraction principle. — [design](2026-05-14-gedcom-phases-split-design.md) — [plan](2026-05-14-gedcom-phases-split.md)

### Duplicates split (2026-05-14, archived same day)
Roadmap §3.1 item. Decomposed `src/api/duplicates.ts` (1,649-LOC monolith with 4 entity dedup families) into per-entity sibling files under `src/api/duplicates/`. Persons (403 LOC), places (434), sources (357), media (441), `shared.ts` (34, holds `levenshtein` — extracted because grep showed places + sources + media all call it), `index.ts` (9, barrel re-export). Monolith deleted; consumers' `import from '../api/duplicates'` resolves to `duplicates/index.ts` via Node module resolution. Each entity's per-entity normalizer (`placeNormalize`, `sourceNormalize`, `mediaNormalize`) stays private to its own file — entity-specific intent. Test files updated to import from the leaf paths (`'../../src/api/duplicates/persons'` etc.); the FK-self-check tests that use `readFileSync` also repointed at the new paths. 134/134 dedup tests pass; no perf regression (29.77s suite same shape as before). No factory pattern, no class hierarchy — flat sibling files are enough per the design's explicit rejection of premature abstraction. Helper-extraction (`shared.ts`) followed the "discover, not assume" rule: extracted ONLY `levenshtein` because three entities use it identically; per-entity normalizers stay separate because they encode entity-specific behavior. — [design](2026-05-14-duplicates-split-design.md) — [plan](2026-05-14-duplicates-split.md)

### Panel danger-zone extraction (2026-05-14, archived same day)
Roadmap §3.7 item, reframed from the audit's original (wrong) premise. Audit said "extract `usePanelSections`" — verified that composable already exists and 8 of 10 panels already use it. Re-investigation found the real duplication: six entity panels (Person, Place, Source, Media, Group, ResearchTask) each implemented an identical `panel-danger-zone` block (~50 LOC × 6). Plan extracted those into a shared `<PanelDangerZone>` component at [`src/renderer/components/PanelDangerZone.vue`](../../src/renderer/components/PanelDangerZone.vue) with an internal entity-type dispatcher (typed union `'person' | 'place' | 'source' | 'media' | 'group' | 'research-task'` — typo'd entityType is a TS error). Unified i18n keys: `confirmModal.deleteEntity.action/title/body/confirm` + `entities.<type>` in both `sv.ts` and `en.ts`. Six panel migrations (PersonPanel, PlacePanel, SourcePanel, MediaPanel, GroupPanel, ResearchTaskPanel) collapsed inline danger-zone markup + state + handlers into one `<PanelDangerZone>` element each, per-panel LOC drop of 13-15 lines. Verification: `grep -rln 'panel-danger-zone' src/renderer/components/` returns only `PanelDangerZone.vue` — single source of truth achieved. `npx vitest run tests/components tests/unit/components` → 687/687 passing (5.33s). Cascade-summary domain knowledge stays in parent panels (formatted i18n strings with count interpolation); component renders them as `[message]` array via ConfirmModal's `messages` prop. User-visible side-effect documented: confirm-dialog wording is now uniform across panels (previously varied slightly per entity). Future deletion-UX changes (type-name-to-confirm gate, swapped dialog, etc.) edit one file. — [design](2026-05-14-panel-danger-zone-extraction-design.md) — [plan](2026-05-14-panel-danger-zone-extraction.md)

### Vite 8 bundle regression investigation (2026-05-14, archived same day — no regression found)
Roadmap §1.1 item. The 2026-05-14 audit raised the concern that v0.256 builds were "much smaller" than the post-Vite-8 v0.257.4 baseline — and the Vite 8 close-out plan had no comparable Vite 7 measurement to refute it. This plan applied the discipline from [`.claude/rules/plans.md`](../../.claude/rules/plans.md) §"Verification discipline at close-out": measure both sides, diff, decide. Measured pre-Vite-8 baseline at SHA `5b51e75a` (the package.json `0.257.3` commit — no `v0.257.3` tag exists locally) against current `main` (`0.257.6`). **Total `dist-tauri` size unchanged: 72M / 72M (0% delta).** Every metric within ±5% of baseline except aggregate JS chunk count (+125%, 48 → 108), aggregate CSS chunk count (+42%), and aggregate JS bytes (+9.8%, 2160 KB → 2372 KB) — but the per-chunk sizes shrank materially (`tauri-window-api`-* dropped −76.8%, 552K → 128K). Vite 8's Rolldown chunker produces finer granularity for essentially the same total payload, in 5.3× less build time (4.23 s → 0.80 s). User's "v0.256 was much smaller" recollection cannot be the Vite 8 upgrade — if real, it predates v0.257.3 entirely (likely the lazy-gazetteer rework at commit `1e2db02e` that ended the eager-collapse era). Decision: `accepted-as-structural-no-action`. Tasks 5–7 (the three tunings) skipped per the Task 4 gate. — [design](2026-05-14-vite-8-bundle-regression-design.md) — [plan](2026-05-14-vite-8-bundle-regression.md) — [RCA](2026-05-14-vite-8-bundle-regression-rca.md)

### Tauri-port RCA + post-Tauri audit synthesis (2026-05-12, archived 2026-05-13)
Retrospective on the Tauri full-port close-out that surfaced 18 gaps across four root causes — RC1 (close-out skipped legacy delete, leaving `npm start = electron-forge start` broken), RC2 (renames misclassified as cosmetic — proof-of-concept crate name in Cargo.toml + bundle identifier + Mach-O binary + Spotlight identity), RC3 (eager `import.meta.glob` shipped to prod, OOMing `npm run build` on default heap and pinning the renderer at 4 GB / 100% CPU at boot), RC4 (decorative CI + unrun Playwright + 75 unpushed commits). Six structural mitigations landed in rules + workspace: L1 user-goal-falsifiability test, L2 cleanup-in-scope-when-load-bearing, L3 smoke-antipattern ban (noun + verb + identifier), L4 mandatory `superpowers:verification-before-completion` invocation, L5 project-rules-go-in-workspace, L6 direct-push-to-main requires local-green (PR-based work uses CI as the check), L7 e2e is load-bearing verification (broken e2e blocks archive). Companion synthesis doc `2026-05-12-audit-recommendation.md` captured the measured state of the codebase (test count, lint state, unpushed commits, bundle size, RSS at boot) and the recommended execution order across the seven plans the RCA spawned; all seven are now archived (`skipped-tests-cleanup`, `gazetteer-lazy-chunks`, `e2e-test-repair`, the crate-rename shipped in 0.254.0, `vite-7-upgrade` plus its follow-up `vite-8-upgrade`, `app-a11y-gaps`, `bulk-api-functions`). Gap #16 (`release.yml` audit) audited clean 2026-05-13 (triggers on `main`, generic cache keys, zero retired-name references, repo URL matches remote). Memory-cleanup gaps A–C and any residual code-health work folded into the 2026-05-14 audit follow-up roadmap. — [RCA](2026-05-12-tauri-port-rca.md) — [synthesis](2026-05-12-audit-recommendation.md)

### Bulk api/ CRUD functions for the GEDCOM / Holger / RootsMagic hot paths (2026-05-12)
Follow-up to the bulk-insert batching plan below: that plan migrated the importers writing through raw `stmt.run([...])` (Genney, Gramps, `consolidateMediaFolder`) but explicitly deferred the importers writing through the api/ CRUD layer (`createPerson`, `addPersonName`, `createEvent`, `createCitation`, etc.) because each api/ function did one or more IPC roundtrips per call. This plan added bulk siblings to those api/ functions and migrated the api/-layer hot paths to the same collect+flush shape. Five api/ entity groups grew bulk variants: `persons.ts` (`bulkCreatePersons`, `bulkAddPersonNames`, `bulkAddPersonIdentifiers`), `relationships.ts` (`bulkCreateRelationships`, `bulkAddEventParticipants`), `events.ts` (`bulkCreateEvents`), `sources.ts` (`bulkCreateSources`, `bulkCreateCitations`), `media.ts` (`bulkCreateMedia`, `bulkAddMediaLinks`). All take rows with `id?` pre-allocated via `crypto.randomUUID()` so FK references resolve through caller-side maps without waiting for inserts. Importer migrations: `src/import/gedcom/phases.ts` rewired every per-INDI / per-FAM / per-OBJE / per-SOUR loop to collect+flush in FK topo order (persons → person_names → person_identifiers → events → event_participants → citations → media → media_links → relationships); `src/import/gedcom/event-importer.ts` kept its singular function names but its callers in `phases.ts` buffer the results and flush via `bulk*`; `src/import/rootsmagic/transform.ts` (the close-out commit, 2026-05-13) migrated all 13 of its per-row api/ callsites into the same shape, with `findOrCreatePlace` kept singular per the plan's deviations (place creation is interleaved with lookup; places are bounded in count). Scope deviations: `groups.ts` / `repositories.ts` / `research_tasks.ts` did NOT get bulk variants (importers don't write those entities in row-loops); per-function unit tests for the bulk variants weren't added (coverage is via the importer integration tests + `import-batching.test.ts`); golden-test against the singular path was skipped (per-entity tests already exercise correctness of the underlying writes). User-observable outcome verified 2026-05-13: "imports are blazingly fast" (user confirmation). Also resolved the 0.257.3 / 0.257.4 manifest drift the 2026-05-12 audit flagged — `Cargo.toml`, `Cargo.lock`, and `tauri.conf.json` re-synced with `package.json` at 0.257.5. Plan landed piecemeal across commits (the bulk variants + GEDCOM phases migration shipped with earlier perf work; the rootsmagic piece was the named close-out commit); recorded in the plan's "Tasks discovered during execution" as a shape future plans should avoid (single PR per plan per [`.claude/rules/plans.md`](../../.claude/rules/plans.md) L6). — [plan](2026-05-12-bulk-api-functions.md)

### Bulk-insert batching — kill the per-row IPC roundtrip in importer hot loops (2026-05-12)
Closed the multi-hour-import regression the Tauri port introduced. Pre-Tauri (Electron + in-process node-sqlite3-wasm), every importer hot-loop `stmt.run([...])` was a ~10 µs WASM call; post-Tauri, the same call is a ~1 ms full IPC roundtrip (renderer → tauri-invoke → rusqlite → return). For a 1.5 GB Holger DB with millions of rows that turns minutes into hours. Added `db_batch_run` Rust command (`src-tauri/src/db.rs` + `lib.rs`) that takes one SQL string + many parameter rows, holds the connection mutex for the whole batch, prepares the SQL once via `prepare_cached`, iterates rows under the lock, and propagates any mid-batch error so the surrounding JS `BEGIN/COMMIT` ROLLBACKs atomically. Renderer-side `Statement.runBatch` (in the Tauri shim at `src/renderer/db-shim.ts`) plus backend-agnostic `runBatch` / `runBatchOnStatement` helpers in `src/api/db.ts` give one shape across both backends — the Electron / node-sqlite3-wasm path falls back to a sync per-row loop where the IPC cost being avoided doesn't exist. Migrated importer hot paths: Genney transform's 16 `stmts.X.run([...])` callsites collapse to per-section `flushAll()` of accumulated parameter buffers (16k per-row IPC calls for a 1000-person fixture became 9 batched calls — measured); gramps' `parent_place_id` UPDATE loop becomes one `runBatch`; `consolidateMediaFolder`'s 12k per-row `UPDATE media SET file_ref = ?` rewrites batch per-worker (8-worker pool × 1000-row flushes). New regression test `tests/unit/import-batching.test.ts` mocks `@tauri-apps/api/core`'s `invoke` to route db_* calls through a real in-process node-sqlite3-wasm DB while counting calls by command name; asserts wall-clock < 8 s and `db_batch_run` count stays constant (not row-proportional) — the falsifiability anchor that catches a future per-row regression on a fast machine. `.claude/rules/api.md` "SQLite bulk-write performance" grew the mandatory "Use runBatch" clause. Cargo tests (`db_batch_run_inserts_many_rows_and_caches_prepare`, `db_batch_run_propagates_mid_batch_failure`) verify the mutex hold + error-propagation contract on the Rust side. Two scope deviations documented in the plan's Tasks-discovered: `importSplace` (Genney recursive places — small absolute count, per-row IPC stays acceptable), and the GEDCOM phases + RootsMagic transform (they call api/ functions per-row, not raw `stmt.run`; clean batch migration requires bulk variants of the api/ CRUD layer — its own follow-up plan). The dominant late-stage cost on a real 1.5 GB Holger import (consolidateMediaFolder) is delivered by this plan; per-row api/ call cost during the GEDCOM-import phase remains for the next plan to address. — [plan](2026-05-12-bulk-insert-batching.md)

### E2E expansion — Tier 1 in CI + Tier 2 panels/reactivity/imports local (2026-05-13)
Two-tier Playwright setup catching panel-CTA, reactivity, and importer regressions before they reach the running app. Tier 1 (`npm run test:e2e`, 4 projects: boot/crud/website-export/duplicates) is now wired into CI on every PR via a new `e2e` job on `ubuntu-latest` with `xvfb-run` — pre-plan CI ran lint + unit only, so the "Tier 1 gates PR" outcome was aspirational; this plan makes it real. Tier 2 (`npm run test:e2e:full`) adds three new projects: `panels` (data-driven Surface Contract checks — host-flows-in, fulfills-label, lifecycle-parity, no-degradation — across all 8 right-side panels via `tests/e2e/fixtures/panels.ts` PanelDescriptor entries), `reactivity` (14 triples in `tests/e2e/fixtures/reactivity-triples.ts` covering every paneled view, list view, and the chart for `data-changed` propagation), and `imports` (6 cases in `tests/e2e/imports.spec.ts` round-tripping GEDCOM 5.5.1 + 7.0 + per-dialect Holger/RootsMagic/Gramps/Family Tree Maker exports). Tier 2 stays local + on-demand until the repo goes public OSS with free CI minutes; CLAUDE.md close-out evidence rule now requires `test:e2e:full` output for any UI/importer/`data-changed`-touching plan. New `SLAKTFORSKNING_HEADLESS=1` env (lib.rs:740-790) makes the e2e binary launch with off-screen window + macOS `NSApplication.setActivationPolicy(.Accessory)` + Windows `skip_taskbar(true)` — 60+ panel tests no longer pop 60+ Dock-icon-having windows that steal focus on macOS; runs cleanly under `xvfb-run` on Linux CI. Helpers in `tests/e2e/helpers/`: `mutate-via-mcp.ts` (dot-path-against-window.api with `onDataChanged` wait), `seed-host-entity.ts` (verified `<domain>.create` shapes), `panel-descriptor.ts` (types). Stale 11-project Electron-era e2e description in `.claude/rules/tests.md` replaced. Plan close-out captured six deliberate-break verifications proving user-goal-falsifiability: `+ Event` → `+ Bogus` makes the CTA-label test red; `:person-id="undefined"` on EventModal makes host-flows-in red; commenting out the row trash `<td>` makes lifecycle-parity red; an early `return` in SectionHeader.onAction when collapsed makes no-degradation red; commenting out `window.api.onDataChanged` in `usePagedList.ts:267` makes 3 PersonsView/PlacesView/SourcesView reactivity tests red; `throw new Error('e2e-canary')` in `importGedcom` makes both GEDCOM import tests red. Each capture revert-clean verified. Scope deviations: RelationshipPanel doesn't exist (relationships live under PersonPanel → Relations); ExportOptionsPanel excluded per its own leading HTML comment; Genney importer TODO'd because `api.import.genneyRun` is `notWired` in the Tauri build; native binary importer fixtures deferred to a follow-up. — [plan](2026-05-13-e2e-expansion.md) · [design](2026-05-13-e2e-expansion-design.md)

### E2E framework follow-ups — seed enrichment, picker bug fix, native-binary deferral codified (2026-05-14)
Closed the test-coverage gaps and one real product bug the e2e-expansion framework surfaced. The original framework shipped with 11 lifecycle-parity tests skipped at runtime (sections where the seed step didn't pre-populate a row) plus two TODO'd product issues. This plan converts each gap into either coverage or an explicit deferral with concrete trigger conditions. **Task 1 — seed-step row enrichment:** extended `tests/e2e/fixtures/panels.ts` so every `PanelDescriptor.seed` pre-populates rows for sections that carry a check-3 (lifecycle parity) entry — PersonPanel.Groups + Tasks (via `groups.addLink` + `researchTasks.addLink`), PlacePanel.Tasks, GroupPanel + ResearchTaskPanel.Places + Media, MediaPanel.Linked Places. `panel-surface.spec.ts` `getSectionRowCount` now also recognises `.linked-row` (LinkedXSection pattern). **Task 2 — native-binary deferral:** added an explicit `// Deferred coverage` block above `CASES` in `imports.spec.ts` naming the missing native formats (.gpkg / .rmtree / .gramps / .gcc / .backup) and the two un-defer triggers (regression escapes / contributor with source apps). **Bonus — picker bug fix:** replaced PersonPanel's inline `showGroupPicker = !showGroupPicker` toggle and MediaPanel's `showXPicker = true` assignments with `openXPicker()` helpers that auto-expand the section before opening the picker — the previous handlers silently no-op'd when the section was collapsed (Surface Contract "no silent degradation" violation, found by the new framework). `panel-surface.spec.ts` check 4 now covers `kind === 'picker'` sections (was TODO'd). **Bonus — replace runtime skip with descriptor opt-out:** introduced `PanelSection.skipLifecycleParity?: { canonicalOwner, reason }` requiring authors to name the canonical sibling section that owns the primitive's lifecycle. PersonPanel.Timeline / PlacePanel.Timeline / PersonPanel.Life map all opt out citing Events. No more silent runtime skips: sections that don't opt out hard-assert `rowCount > 0` so a broken seed fails fast with a clear message. Genney `notWired` was re-triaged: a clean Tauri-side wiring needs either a Node sidecar (1-2 weeks) or a Rust Derby reimplementation — not a small fix. Scoped out, documented for a future plan. Net result on `--project=panels`: from 126 passed + 11 skipped → **144 passed + 0 skipped** (+18 active Surface Contract assertions, 0 silent gaps). — [plan](2026-05-14-e2e-framework-followups.md)

### Post-audit e2e regression triage — Specta rename_all sweep (2026-05-15)
The first Tier 2 e2e close-out run against post-audit `main` surfaced 3 failures (Holger imports, PlacePanel Events save, ChartView reactivity). Investigation by the Genney agent identified F1's structural root cause by reading `tauri-macros-2.6.1/src/command/wrapper.rs:60–122`: `#[tauri::command(rename_all = "camelCase")]` renames *parameters only*, not the command name. Tauri's registry stores the literal Rust ident (e.g. `holger_extract_ged`); the Specta-generated binding emits `__TAURI_INVOKE("holgerExtractGed", ...)`. Mismatch → every `rename_all` command is unreachable through the auto-generated Specta wrapper. Plan Option A (chosen): drop `rename_all = "camelCase"` from all 15 affected `#[tauri::command]` annotations across `src-tauri/src/lib.rs` (12) and `src-tauri/src/media.rs` (3); rename Rust function parameters to camelCase directly (`#[allow(non_snake_case)]`) for the load-bearing commands (`holger_extract_ged`, `holger_bulk_copy_media`, `holger_consolidate_media`, `media_thumbnail`, `website_export_media`, `website_bake_preview_thumbnails`, `db_batch_run`); single-word-param commands (`db_*`, `secondary_db_*`, `fs_write_temp_bytes_base64`) drop `rename_all` without param renames (`to_lower_camel_case()` on single-word idents is idempotent). Regenerated `src/renderer/bindings.ts` via `cargo test --lib export_specta_bindings`. F2 demoted to test-bug — the panel-surface helper now clicks the first quick-segment when none is pre-selected (PlacePanel's `+ Event` doesn't pre-fill `event_type` via `suggestNextEventType` the way PersonPanel does); no production change. F3 demoted to flake/environment after clean-rebuild repro showed 5/5 + 1/1 green. Deliberate-red verified F1's fix is load-bearing: re-adding `rename_all` to `holger_extract_ged` re-produces the failure verbatim. Result: `npm run test:e2e:full` → **170 passed, 0 failed** across all 7 projects. — [plan](2026-05-14-post-audit-e2e-regression-triage.md)

### Genney Tauri wiring — last `notWired` importer goes live (2026-05-15)
Replaced `api.import.genneyRun = notWired('Genney')` in `src/renderer/tauri-window-api.ts` with a real polyfill. Architecture: Bun sidecar mirroring the MCP sidecar shape — `scripts/build-genney-sidecar.mjs` bundles `src/import/genney/sidecar-entry.ts` (Bun entry that opens SQLite via `node-sqlite3-wasm` and runs `importFromGenney` with NDJSON progress on stdout) into `dist-genney/genney-import.bundle.mjs`; new `src-tauri/src/genney.rs` spawns it via tauri-plugin-shell and streams the result; new `genney_import` Rust command in `lib.rs::collect_commands!` (dropped `rename_all` during regression-triage merge to match the rest of the codebase). Renderer-side polyfill calls `commands.genneyImport(...)`, replays buffered progress through `fireProgress('genney', …)`, falls back to GEDCOM import path when the archive carries no Derby DB but a `.ged` inside (encrypted `.gcc` or `.backup`), calls `holgerConsolidateMedia` to normalise file_refs per `.claude/rules/media.md`, fires `data:changed`. `tauri.conf.json` `beforeBuildCommand` chain runs the new sidecar build; resources package the bundle + Java extractor jars. `GENNEY_LIB_DIR` env var makes the JAR cache location work under the Tauri sandbox (`app_cache_dir/genney/lib`). Tiny `genney-small.gcc` fixture (263 bytes, 3 INDIs) committed under `tests/e2e/fixtures/imports/`; `tests/e2e/imports.spec.ts`'s `genney-gcc` case un-TODO'd. `api.import.genneyDiscover` and `api.import.onHolgerProgress` (line 1001 override) stay deferred with explicit `Deferred:` comments + un-defer triggers — the `notWired(label)` helper is now caller-less. — [plan](2026-05-14-genney-tauri-wiring.md)

### `build-third-party-licenses.mjs` reliability — clean skip when node_modules is empty (2026-05-15)
Subagents working in fresh worktrees (and any contributor doing a clean clone before `npm install`) used to see `npm ls --omit=dev --all exited 1 (ELSPROBLEMS)` cascade out of `build:third-party-licenses`, polluting every dispatch's report with a "this is pre-existing, unrelated" paragraph that had to be diagnosed and discounted. The script now mirrors its existing "cargo license missing → warn + emit npm-only output" pattern for the new case: if `node_modules` is missing OR empty (`!existsSync(NODE_MODULES) || readdirSync(NODE_MODULES).length === 0`), it warns once, writes a self-explanatory placeholder `THIRD_PARTY_LICENSES.txt` (placeholder names `npm install` + `npm run build:third-party-licenses` as the remediation), and exits 0. CI always runs `npm ci` before the bundle build, so shipped releases continue to produce the real 821-package list (183 npm + 638 Rust). Bonus: the script's stale "Walks production + Electron runtime deps" header comment was rewritten to accurately describe the npm prod deps + Tauri Rust crates via `cargo license` model, and the *generated* output's stale "Electron and Chromium ship their own license bundle" line was replaced with the correct system-WebView (WebKit / WebView2 / WebKitGTK) description. Two new test cases in `tests/unit/scripts.thirdPartyLicenses.test.ts` exercise the empty-`node_modules` path in an isolated tmpdir. — [plan](2026-05-14-third-party-licenses-reliability.md)

### Genney sidecar tempDir cleanup race — fallback `.ged` survives long enough to read (2026-05-15)
Immediate close-out follow-up to the Genney Tauri wiring plan. `npm run test:e2e:full` against the Genney plan's merged state failed `genney-gcc` with `import returned failure envelope: read: No such file or directory (os error 2)`. The plan's preamble suspected Bun-binary / sidecar-bundle / JAR-cache resource resolution under `tauri build --no-bundle`; all four false trails. Diagnosis (Task 0.1) — the `read:` prefix is project-local (`fs_read_text` / `fs_read_bytes_base64` in `src-tauri/src/lib.rs:362,377`), so the failing call was a renderer-side `fs_read_*`, not a Rust-side `bun` spawn. Traced to `commands.fsReadBytesBase64(result.gedcomFallbackPath)` in `src/renderer/tauri-window-api.ts:1062` — the GEDCOM-fallback branch that runs after the sidecar succeeds. Real cause: `importFromGenney`'s `finally` block (`src/import/genney/index.ts:187-191`) `fs.rmSync`'d `tempDir` even when the early-return branch handed back a `gedcomFallbackPath` pointing **inside** that `tempDir`. Sidecar returns the path, deletes the file it pointed at, renderer reads → `ENOENT`. Fix mirrors the holger-extracted-ged cleanup pattern: before returning the fallback, `mkdtempSync` a sibling `os.tmpdir()/genney-fallback-XXXXXX/` dir, `copyFileSync` the `.ged` into it preserving basename, return that stable path; the renderer's polyfill then `fs_remove_dir`'s the sidecar-allocated parent after consuming the bytes (`src/renderer/tauri-window-api.ts:1069-1086`). Re-activated the `genney-gcc` e2e case (removed the 2026-05-15 TODO block). Tier 2 e2e: **171 passed, 0 failed (2.6m)** across all 7 projects — up from 169 passed pre-fix (genney-gcc was failing, ChartView flaked in the prior run; both green now). Deliberate-red verified the canary at `genney::run_import` reaches the test assertion. Plan body updated with "Diagnosis diverged from plan's suspicions" notes — Task 0.2 (path logging in `genney.rs`) and Task 0.3 (full-bundle comparison) skipped because Task 0.1 located the bug in TypeScript-land. No Rust changes needed. Lesson for future external-binary/resource plans: the `read:`-prefix grep pattern locates renderer-side fs failures faster than enumerating Rust-side resource paths. — [plan](2026-05-15-genney-e2e-path-resolution.md)

### Shared `<AppSidebar>` + `<AppSettingsPanel>` between renderer + static SPA (2026-05-15)
Both `src/renderer/App.vue` (the Tauri shell) and `src/static/App.vue` (the website-export static-SPA shell) used to hand-roll their own sidebar markup + an appearance/theme/text-size/read-aloud/language settings panel — they had drifted apart (static shipped a bespoke `<form class="sidebar-search">` input the real app didn't have; renderer was ahead by `menuLayout` + `addBtnStyle` settings rows). Both now mount the new shared components in `src/renderer/components/AppSettingsPanel.vue` (variant-gated — 7 rows for `renderer`, 5 for `static`) and `src/renderer/components/AppSidebar.vue` (driven by a structured `:sections` prop with a `#bottom` slot for renderer-only chrome — undo row, import-export, settings link). `src/renderer/App.vue` shrank 1091 → 758 LOC; `src/static/App.vue` shrank 389 → 96 LOC. Static SPA's bespoke search input is replaced by a `🔍 Sök` router-link matching the renderer's pattern. Renderer's horizontal-topbar mode is explicitly out of scope (static never ships horizontal); its settings panel still adopted `<AppSettingsPanel>` so the two settings-panel sites in App.vue no longer duplicate. Structural identity is regression-tested by `tests/components/app-sidebar-shape.test.ts` (asserts both variant section sets produce the same outer chrome + `AppSettingsPanel` rendered as a sub-component) and `tests/components/app-settings-panel.test.ts` (asserts shared rows for both variants + renderer-only gating + persistence/DOM-class side effects). Both new components ship with self-contained scoped CSS so they survive the App.vue style-block cleanup that landed in the same waves. localStorage keys (`slaktforskning-appearance`, `slaktforskning-theme`, `textSize`, `slaktforskning-nav-orientation`, `slaktforskning-add-btn-style`) preserved as string literals across both consumers — no settings reset for existing users. Per-task code review caught a topbar-popover positioning regression mid-wave (the `.topbar-settings-panel` `position: absolute` rule had no replacement when its old markup was deleted) — fixed by retargeting via `.topbar-settings .settings-panel { … }` in the same wave. Bonus close-out cleanup: 14 `:aria-checked="String(...)"` patterns rewritten to `:aria-checked="..."` to satisfy Vue's stricter Booleanish typing. — [plan](2026-05-15-shared-app-sidebar.md)

### Website-export preview perf — 4× faster on large databases (2026-05-15)
The author reported that opening the website-export preview against their 22k-person family.db took "ungefär en minut". Plan opened with a measure-first/early-exit clause: instrument with `[preview-timing] <phase>: NNN ms` console logs gated on a localStorage flag, run an actual BEFORE measurement via the dev-MCP bridge against the real database, decide whether to optimise. BEFORE measured at 22.8 s (`previewSnapshot` first pass) / 58.9 s (second pass) / 74.6 s (`buildPreviewHtml` parallel branch) — well over the 30 s target, optimisation proceeded. Two changes carry the win: (1) `src/api/html_site/scope.ts` replaces per-frontier-person ancestor/descendant queries with one bulk `IN`-list query per generation (also bulk-batched the final spouse lookup) — N+1 → 1 per generation. (2) `src/api/html_site/snapshot.ts` and `src/api/html_site/preview.ts` short-circuit JS-side scope filtering when the user picks "everyone" without `excludeLiving` — every row would pass the filter anyway, so the `.filter(...)` walks across 22k persons / 37k relationships / 48k events / 70k participants / 12k media_links are skipped entirely. Result: previewSnapshot 22.8 s → 5.8 s first pass, 58.9 s → 13.7 s second pass — well under target. **`Promise.all`-parallel-query variant was tried and reverted** (commit `0118f7e1` carries the reasoning): on a single-SQLite-connection backend the 13 parallel queryAll calls all queue on the same mutex AND starve concurrent quality-check IPCs of any slot, making the preview ~70% slower and pushing one quality check from 317 ms to 109 s. Scope deviations honoured: Task 4 (Rust-side `rayon` parallel thumbnail bake) skipped — the previewSnapshot win is large enough that thumbnail-bake time (1–5 s sequential) is no longer the user-visible bottleneck; deferred. Snapshot-stability test (the Prime Directive fidelity guard the plan called for) skipped because the optimisations are pure JS filter-skip + bulk SQL — they cannot change content, only how-fast; verified by the existing `tests/unit/snapshot.test.ts` + `tests/unit/scope.test.ts` + `tests/unit/html_site-preview.test.ts` suite (27 tests green). — [plan](2026-05-15-website-preview-perf.md)

### Gazetteer resolver disambiguation — famous-city, language-overlay, country-qualifier (2026-05-21)
When a genealogist types a city name in a foreign-language spelling (`Köpenhamn`), an English form (`Copenhagen`), or with a country qualifier (`Köpenhamn, Danmark`), the place pin should land at the actual famous city — not at a Swedish farm of the same name, not at Copenhagen NY (population ~700), not at the Denmark country-center. Discovered while populating the OSS-launch demo for the Swedish royal House of Holstein-Gottorp: Sofia Magdalena's birth at "Charlottenborgs slott, Köpenhamn" pinned 200 km away on a Swedish farm. T03 investigation found a single root cause across all three reproduced bugs (plus the `Richmond, Kalifornien USA` saved-memory cautionary tale): `applyTranslations` in `src/api/place-gazetteers/merge.ts` silently drops overlay translations whose full path key doesn't exist in the merged tree — `lang-sv-geonames` keys the Copenhagen entry against `World › Europe › Denmark › Capital Region › Copenhagen` while `dk-sogne-dawa` puts the city under `Region Hovedstaden › København`, and the `(name, type)` node-merge keeps them as siblings under Denmark, so the alias never attaches. **Fix:** partial-path + name-or-alias descendant lookup in `applyTranslations`: when the overlay's full path misses, peel from the right to find the deepest existing prefix, BFS that subtree for a node whose name OR existing aliases match the path's last segment, attach both the translation value AND the last segment as aliases on the matched descendant. Single-subtree scope guarantees `Köpenhamn` doesn't accidentally attach to Copenhagen NY. The plan's original three-fix scope (T04/T05/T06 as separate bugs) collapsed to one fix after the investigation; T05 fully absorbed, T06 reduced to regression verification. Test-harness fix in `tests/unit/gazetteer-disambiguation.test.ts`: use `loadGazetteers(...)` (merged tree, matching the running app's code path) instead of raw `getAllGazetteers()` — otherwise the merge-layer alias attachment is invisible to assertions. Close-out evidence: 5/5 disambiguation tests green (Köpenhamn / Copenhagen / Köpenhamn,Danmark / Charlottenborgs slott,Köpenhamn / Richmond,Kalifornien USA all assert specific lat/lon boxes for the actual famous city); 229/229 gazetteer suite green (including the Pitcairn-Skottland anchor tests that rule 1 of `isBetterCandidate` was designed for — proves rule 1 was untouched); 4381/4381 full `npm test`; `npm run lint` 0 errors; `npm run build` finished release profile in 1m 27s, bundled .app + .dmg. T08 diagnostic script and T09 manual UI verification deferred — the 5 mechanical user-goal assertions are the load-bearing falsifiability check per the plan's user-goal-falsifiability test; manual map check happens on next-app-launch after merge. — [plan](2026-05-21-gazetteer-resolver-disambiguation.md)

### Closed without execution — modal companion composables (2026-05-31)
Closed by maintainer review, not by execution. Plan 3.5 (modal composable extraction) shipped 14 composables and met its user goal; this follow-up extracted five more (`useCompanionBaptism`, `useNameChangeCompanion`, `useSpousePicker`, `useCitationsForPersonName`, `useWeddingOffer`, `useOverlapCheck`) targeting per-modal LOC drops to bring EventModal 1042→750, NameModal 630→450, RelModal 504→400. Reviewed 2026-05-31: those LOC targets were the 3.5 archive's "aspirational follow-up" — i.e. they weren't load-bearing for any user-observable outcome. No modal feature is blocked by current LOC. Companion / wedding / overlap bugs DO get found and fixed in the modal files today. The plan's user goal ("a bug in companion-baptism logic lives in `useCompanionBaptism.ts`") is developer ergonomics dressed as a user goal. Reopen trigger: a real modal bug whose root cause is hard to locate because the file is too big. — [plan](2026-05-14-modal-companion-composables.md)

### Closed without execution — renderer-side perf baseline (2026-05-31)
Closed by maintainer review, not by execution. The plan's user goal was a renderer-side baseline (Safari Web Inspector .cpuprofile traces for boot, place-resolve, dedup) to pair with the Rust-side `samply` baselines plan 1.2 captured. Original consumer was Tier 3 refactors (3.1, 3.2, 3.4) which needed something to verify against — all six Tier 3 refactors (3.1, 3.2, 3.3, 3.4, 3.5, 3.7) shipped without it. Reviewed 2026-05-31: a baseline captured today against today's codebase is fresher than one captured weeks before the next user complaint arrives — on-demand-against-real-repro is more honest than on-shelf. Reopen trigger: next user-reported renderer perf complaint. The capture-against-the-repro takes the same 30 minutes the plan estimated and produces a more useful trace because it's keyed to the real workload. — [plan](2026-05-14-perf-baseline-renderer.md)

### Audit follow-up roadmap close-out (2026-05-31)
Index document for all post-audit work; written 2026-05-14 to translate the 2026-05-12 audit recommendations into executable plans. Now closed: every Tier 1 strategic plan shipped (1.1 Vite 8 regression, 1.2 perf baseline Rust-side, 1.3 Specta migration, 1.4 kkrpc + Bun sidecar). Every Tier 3 complexity refactor shipped (3.1–3.5, 3.7). Tier 2 tactical sweeps either shipped or were verified already-clean. The two remaining items at close-out are explicitly dropped: **T2.5 Raw Payloads** — speculative; no IPC slowness complaint exists; the audit's track record on speculative items (Vite 8 "regression" was nothing, chart-layout audit premise was wrong, 3.7 `usePanelSections` was already done) argues against carrying speculative work indefinitely; reopen on a specific complaint. **T2.6 active-plans archive sweep** — being executed *as* this close-out (the four 2026-05-14 plans that survived to today are all archived in this same commit). Reopen trigger for T2.5: a specific IPC bandwidth or roundtrip complaint with a measurable workload. — [plan](2026-05-14-audit-followup-roadmap.md)

### Closed without execution — importer binary fixtures (Holger + RootsMagic half) (2026-05-31)
Closed by maintainer review for the Holger + RootsMagic portion only. Original plan covered four native binary fixtures: Holger `.zip+media`, RootsMagic `.rmtree`, Gramps `.gramps`, Gramps `.gpkg`. Holger and RootsMagic require paid Windows software the maintainer doesn't run; the plan correctly flagged "Authoring requires running the source apps." Reviewed 2026-05-31: this is contributor-driven work, not maintainer backlog. Permanent close for Holger + RootsMagic: re-shape the `// Deferred coverage` comment in `tests/e2e/imports.spec.ts` to read as a contributor-issue trigger ("if you have RootsMagic or Holger installed and want to harden import coverage, open an issue with a 3-person test fixture attached and we'll wire it in") rather than as ongoing backlog. Gramps half stays alive in a fresh focused plan — see [2026-05-31-gramps-binary-fixtures.md](../2026-05-31-gramps-binary-fixtures.md) — because Gramps is open-source, free, and multi-platform, so the maintainer can author those fixtures directly. — [plan](2026-05-14-importer-binary-fixtures.md)

### Media → source links (rapport 104, Framing B) (2026-06-02)
User goal: Ben opens a media item, sees a "Källor" section listing the sources it's linked to, links an existing source (or creates-and-links one in a step), unlinks, sees the link reciprocally on the source's own panel — and nothing is lost across a GEDCOM export + re-import. Shipped Framing B of the [media-citations design](2026-05-31-media-citations-design.md): surfaced the pre-existing `media_links(entity_type='source')` association as a 4th linked-entity section on `MediaPanel.vue` (mirroring Persons/Places/Events — zero schema change), reusing `SourcePicker` and the generic unlink. **Key delta from the original plan:** a code-quality review caught that the plan's "already lossless / covered by existing tests" claim was false — the exporter emitted no `OBJE` under `SOUR` and the golden test excludes `media_links`, so the link was silently dropped on export (a Round-Trip Fidelity Prime Directive violation). Scope was corrected (with a nod) to *make* it lossless: the exporter now emits `OBJE` under `SOUR` and the importer reads it back, covered by a new per-field round-trip test for both 5.5.1 and 7.0; the `media_links.entity_type` registry prose was corrected. Framing A (per-media citation FK) stays deferred (see PLAN.md "Considered, not now"), as does GEDCOM-7.0 inline-vs-pointer OBJE conformance (pre-existing across all five entity types). Close-out evidence: `npm test` 4395 passed (51s); `npm run build` bundled `.app`+`.dmg` (vite built in 897ms, Rust release 21.84s, exit 0); `npm run test:e2e:full` 172 passed (2.7m) across all 7 projects incl. the new `[panels]` link-add+reciprocal test; lint 0 errors; vue-tsc clean. — [plan](2026-05-31-media-source-links.md) · [design](2026-05-31-media-citations-design.md)

### GEDCOM 5.5.1 + 7.0 alignment — close every silent round-trip gap in schema + importer + exporter + UI (2026-05-20)
Per-version round-trip fidelity per the Prime Directive: GEDCOM 7.0 is lossless on every authored field the spec carries; GEDCOM 5.5.1 is lossless or documented `lossy:5.5.1-spec-limit` per spec capability. Audit (`docs/GEDCOM_AUDIT.md`) catalogued every silent loss between the DB, the importer, and the exporter; this plan closes them all across five phases. **Phase 1 — schema + exporter corner cases (T02–T03):** six new tables added — `notes` + `note_links` (SNOTE; shared notes attachable to any entity), `person_associations` (ASSO without event mediation), `name_translations` + `place_translations` (TRAN per 7.0 §LANG), `source_coverage_events` (SOUR/DATA/EVEN) — plus two new columns on `events` (`is_negation`, `negation_event_type`), `persons.sex='X'`, `header_metadata` JSON in `db_settings`, and the legacy `sources.repository` free-text column dropped in favour of FK-only `source_repositories` with importer-side structured-Repository synthesis from `_REPO_TEXT`. Exporter patched five corner cases: single-parent FAM emission (orphan parent_child rows now emit synthetic FAMs with sex-appropriate HUSB/WIFE slot), PEDI subtype per-parent correctness (per-parent collection + sort-order trick + 7.0 `3 _PARENT` disambiguator), FAMC/FAMS emission on INDI records, same-couple-twice round-trip (separate `couple` rows), multi-parent triad (`ASSO ROLE PARENT` on 7.0; lossy with disclosure on 5.5.1). **Phase 2 — six new GEDCOM 7.0 concepts (T04–T09):** notes, associations, negations, translations, source coverage, sex X + HEAD + extended date qualifiers all flow through importer + exporter + DB. **Phase 3 — buried-field surfacing (T10–T19):** Repositories CRUD (new view + panel + modal + source↔repo link section); Citations sections on Person/Place/Relationship panels; modal field surfacing for `call_number`/`abstract`/`place_address`/`cause`/`date_value_end`/`name_prefix`/`name_suffix`/`name_qualifier`/`patronymic_base`/`event_participants.role`/`research_tasks.result`/`relationships.subtype`/`media.format`. **Phase 4 — UI for new schema (T20–T24):** shared notes section across every entity panel + NoteModal + NotePicker; person associations section with role picker on PersonPanel; negation toggle on EventModal; translations on PersonNameModal + PlacePanel; source coverage on SourcePanel. **Phase 5 — adjacent surfaces (T25–T27):** Holger / Genney / RootsMagic / Gramps importers audited and mapped — Gramps gets the richest mapping (shared notes via `<note>`+`<noteref>`, ASSO via `<personref>`, alt names + alt place names via lang attribute), RootsMagic gains shared notes from `NoteTable` + witness role mapping, Genney gets `SOURCE.NOTE` → shared notes, Holger inherits everything via the `profile='holger'` GEDCOM importer; archive `.zip` round-trip extended to cover every new table; HTML website export inherits via shared renderer components — `static-api.ts` extended with read-only methods matching `window.api` exactly, NO new static-only components per the design's anti-pattern guard. Round-trip fidelity is now mechanically guarded by 415 per-version tests across 11 files (`gedcom-roundtrip-corner-cases`, `gedcom-notes-roundtrip`, `gedcom-associations-roundtrip`, `gedcom-negations-roundtrip`, `gedcom-translations-roundtrip`, `gedcom-source-coverage-roundtrip`, `gedcom-sex-x-roundtrip`, `gedcom-head-preservation`, `gedcom-date-qualifiers`, `gedcom-fidelity-per-field`, `archive-roundtrip-newconcepts`). `relationships → families` rename deferred to a future plan per `docs/GEDCOM_AUDIT.md` §3 — the exporter corner-case patches close the Prime-Directive gap without it; rename is model-cleanliness work, not load-bearing. Close-out evidence: `npm test` 4376 passed; `npm run lint` 0 errors / 39 warnings; `npm run build:bin` exit 0 in 22.80s; `npm run build:mcp-sidecar` exit 0; `npm run build:static` built in 583ms; per-version round-trip suite 415 passed in 3.13s. Audit: [docs/GEDCOM_AUDIT.md](../../GEDCOM_AUDIT.md). — [plan](2026-05-19-gedcom-alignment.md) · [design](2026-05-19-gedcom-alignment-design.md)

### OSS launch demo + manual (2026-05-21)
User goal: a first-time visitor to the GitHub repo — a 60+ genealogist who has never touched a Tauri app — lands on a README with a real screenshot of the populated app (Swedish royal House of Holstein-Gottorp), follows a sub-10-minute quickstart to build their own first sourced family, and finds a screenshotted `MANUAL.md` covering every panel and importer. Shipped across **v0.264.0–v0.264.7** (`dd432b54`→`2eb109cd`): `docs/screenshot.png` hero shot; `docs/quickstart/01..08-*.png` walkthrough; `docs/manual/*.png` (12 section shots) + `image-credits.md`; `QUICKSTART.md`; `MANUAL.md` (19 sections); README rewrite embedding the screenshot + linking quickstart/manual; and `tests/unit/manual-coverage.test.ts` (mechanical guard: every paneled route in `App.vue` + every importer in `src/import/` has a MANUAL heading). The demo DB was authored entirely via the dev MCP (persons, marriages, ~25 events, sources, repository, face-tagged group portrait). **Deltas recorded at close-out (2026-06-06):** (1) the checkboxes were never flipped — caught as lifecycle drift and archived now; (2) the quickstart was moved out of the README into a standalone `QUICKSTART.md` (`2c90c505`), so T38's coherence test (`tests/unit/readme-quickstart-coherence.test.ts`, 5/5 green) was written at close-out against `QUICKSTART.md` rather than the README — asserting the README→QUICKSTART link, 8 step↔screenshot index matches, file existence, and no orphan screenshots; (3) the *Scope deviations* "no demo DB shipped" was overridden during execution — `examples/swedish-royals.db` **was** committed (`2eb109cd`) as a net-positive first-run demo. Close-out evidence: the plan's own Verification §1 tests are green (`manual-coverage` 4/4 + `readme-quickstart-coherence` 5/5); full `npm test` had 4394 passed / 1 unrelated failure (`scripts.thirdPartyLicenses` — the `vue-router 5.1.0` bump `7a0cd35e` pulls `vite→esbuild@0.28.0` into the prod tree and `@esbuild/darwin-arm64` ships no LICENSE; orthogonal to this plan, tracked as a separate follow-up). Archived per explicit user decision to not gate the doc archive on the unrelated dependency regression. — [plan](2026-05-21-oss-launch-demo-and-manual.md)

### Gramps `.gpkg` archive import (2026-06-06)
User goal: importing a Gramps `.gpkg` package brings in the persons/events **and** writes the bundled media files into `<dbname>-media/` with relative `file_ref`s — from both the desktop file picker and the MCP `import_file` tool. Discovered while executing the paused [2026-05-31-gramps-binary-fixtures](2026-05-31-gramps-binary-fixtures.md) plan: the `.gpkg` "unpack-and-remap-media" branch that plan's Task 2 assumed **did not exist** — the importer only gunzip-or-utf8'd → parsed as XML, so a zip-form `.gpkg` imported nothing (silently, no error) and a tar.gz-form parsed XML by accident while dropping every media file. Empirically proven by running the real importer against all three forms before reshaping the plan. Per `.claude/rules/plans.md` ("scope was wrong from the start → pause, edit, get a nod") the user chose "implement real .gpkg first"; a design spec + this plan were written and executed. Shipped a runtime-neutral extraction core (`src/import/gramps/archive.ts`: fflate `gunzipSync` + USTAR-magic-at-offset-257 detection + `nanotar.parseTar` → `{ xml, media[] }`) and routed both importer entry points (`importFromGramps` path-variant, `importFromGrampsBytes` bytes-variant) through a shared `runGrampsImport` that calls the core, transforms the XML, then writes media via a caller-supplied `GrampsMediaWriter` and rewrites each media `file_ref` to `<mediaFolderName>/<basename>` — mirroring `src/api/archive_import.ts`'s `ArchiveMediaWriter` pattern. The renderer `import.grampsRun` injects an `fs_write_bytes_base64`-backed writer + calls `consolidateMediaFolder`; the MCP `import_file` gramps branch injects a Node-`fs` writer into `getMediaDir(getDbPath())`. `nanotar@^0.3.0` added (zero-dep). The plain `.gramps` XML path is unchanged (media list empty); no schema change; `transform.ts` untouched. **Unblocks** [2026-05-31-gramps-binary-fixtures](2026-05-31-gramps-binary-fixtures.md) Task 2 — the `.gpkg` e2e fixture must now be built as a tar.gz (`tar czf`), not a zip. Close-out evidence: deliberate-red canary made all 6 `.gpkg` unit tests fail with `e2e-canary` then revert→green; `npm test` 4406 passed (48s); `npm run lint` 0 errors; `npm run build` bundled `.app`+`.dmg` (exit 0); `npm run test:e2e:full` 172 passed (2.9m) across all 7 projects. — [plan](2026-06-06-gramps-gpkg-archive-import.md) · [design](2026-06-06-gramps-gpkg-archive-import-design.md)
### Ben feedback polish — rapport 100-103, 105 (2026-05-31)
User goal: when Ben (beta tester, Holger 8 background, limited vision) re-walks his rapport 100-103 + 105 flows on the next release, every friction is gone — the citation form reads "Källhänvisning" with most-reliable-first confidence and a visible dropdown affordance; the event modal shows "(1)" not "1" and trash icons (not ✕) on row deletes; a residence event reads "Inflyttningsdatum"/"Eventuellt utflyttningsdatum" with an "Övriga boende i bostaden" section; adding a participant to an unsaved event offers save-and-continue instead of a dead-end hint; and the person panel surfaces authored data first with Tidslinje/Livskarta moved down to just before Fortsatt forskning and Kvalitet. Shipped as pure i18n + template/markup polish (zero data-model or GEDCOM round-trip impact): A1/A3/A4 citation labels, A2 SourcePicker ▾ chevron (decorative, aria-hidden, real combobox), A5 reversed confidence render order (storage enum `CONFIDENCE_LEVEL_VALUES` unchanged — Prime Directive intact), B1/B2 parens + IconTrash on citation + participant rows, C1/C2/C3 per-event-type residence date/participant labels via a `te()`-fallback computed (no role-enum or schema change), C4 inline save-and-continue (EventParticipantsSection emits `request-save`, EventModal confirms then saves + attaches), D1 "Fortsatt forskning" rename (sv-only; entity-singular "Uppgift" kept per D2; en deliberately unchanged), E1 PersonPanel Timeline/Map reorder. Verified by 5 new component test files (citation labels + confidence order, residence labels, section-count parens + IconTrash, participants save-and-continue, PersonPanel section order), a SourcePicker chevron test (replacing the plan's planned manual screenshot — a test beats manual smoke), and a new `tests/unit/i18n-parity.test.ts` asserting sv↔en key parity. **Deltas at close-out:** (1) main advanced 2 commits mid-execution (oss-launch archival + the `b800260f` license-prune fix) — rebased onto current main, which incidentally resolved the `build:third-party-licenses` test failures that surfaced in the stale worktree; (2) Task 6 left an existing `EventParticipantsSection.test.ts` case asserting the removed save-first hint — updated to the new always-rendered-picker + `request-save` behaviour (caught by the full suite, not the isolated subagent run); (3) a spec-reviewer flagged an orphaned `participantsSaveFirstHint` i18n key + dead `.ep-participants-hint` CSS left by C4 — removed; (4) scope deviations 102 §5, 104, 106 were already resolved separately before this batch. Rapport 102 §4's visual-confirmation-after-save edge (parent may close the modal on `saved`) noted by review as inherent to existing BaseSubPanel wiring, not introduced here; the participant is always persisted. Close-out evidence: `npm test` → 4419 passed (55.7s); `npm run build` → exit 0 (vite built in 1.01s, Rust release 1m28s, .app + .dmg bundled); `npm run test:e2e:full` → 172 passed (2.7m) across all 7 projects ([boot] [crud] [website-export] [duplicates] [panels] [reactivity] [imports]); lint 0 errors. — [plan](2026-05-31-ben-feedback-polish-v215-to-v265.md)

### Gramps native binary importer fixtures (2026-06-07)
User goal: when a regression lands in the Gramps native binary decoder — the `.gramps` (gzipped XML) path or the `.gpkg` (tar.gz, XML + media) unpack-and-write-media path — a test fails in CI before a user notices broken import. Shipped two tiny fixtures (`tests/e2e/fixtures/imports/gramps-small.gramps`, gzipped 3-person XML; `gramps-small.gpkg`, a `tar --format ustar -czf` of `data.gramps` + `media/blank.png`) and wired `gramps-gramps` + `gramps-gpkg` cases into `imports.spec.ts`, both through `import.grampsRun`. Added an optional `expectedMedia` field to the `ImportCase` interface; the `.gpkg` case asserts the bundled media row lands with a **relative `<dbname>-media/<file>` `file_ref`**, exercising the unpack-and-write-media branch end-to-end in the packaged app. Trimmed `.gramps`/`.gpkg` from the deferred-coverage comment (Genney `.backup` / RootsMagic / Holger `.zip` stay deferred) and marked both extensions `binary` in `.gitattributes`. **The new `.gpkg` case immediately earned its keep:** it caught a real regression shipped untested in 0.268.0 — `import.grampsRun` called `consolidateMediaFolder` (Node `fs/promises.mkdir`) in the renderer, so every desktop `.gpkg`-with-media import threw. Fixed by removing the redundant, renderer-incompatible call (the importer's `mediaWriter` + in-importer `file_ref` rewrite already do the work, mirroring `api.archive.import`); released as the 0.269.1 fix alongside the fixtures. **Deltas from the as-written plan** (recorded in the plan's execution note): `.gpkg` is built tar.gz not zip; the importer returns `{ summary }` (not `report.persons`); the real `ImportCase` shape is `fixture`/`apiCall`/`buildArgs`/`spotCheckName` (+ new `expectedMedia`), not the `fixturePath`/`expectedNames` the plan sketched; the deferred comment was already a well-shaped un-defer-trigger block. Close-out evidence: `npm test` 4425 passed (47s); lint 0 errors; `npm run build` bundled `.app`+`.dmg` (exit 0); `npx playwright test` full suite 175 passed (2.7m) across 7 projects, `imports` 9/9 incl. both new gramps cases; deliberate-red — the `.gpkg` case caught the live consolidate bug, and a synthetic `expectedMedia: 2` flip went red ("expected 2 media, got 1") then reverted green. — [plan](2026-05-31-gramps-binary-fixtures.md)

### Chart consistency — shared shell + behaviour parity (2026-06-07)
User goal: the three family-tree charts (Pedigree, Hourglass, Descendants) look and behave identically wherever they share a concept — box rendering, selection, pan/zoom, the "+" add-relative flow, outline placeholders, double-click-to-refocus, keyboard navigation and ARIA — with the shared code in one place so they cannot drift. Reframed (and supersedes) the parked 2026-05-14 `hourglass-layout-refactor` design, whose premise was wrong: the problem was never `hourglass.ts`'s 1,052-LOC *size* but **drift** from the same behaviours being implemented three times. The audit (driven through the running app via the dev-MCP HTTP bridge, since the slaktforskning-dev MCP wasn't connected to the session) confirmed hourglass is structurally the *union* of pedigree (ancestors) + descendant (descendants) + a focal/spouse join — not a different algorithm — and the real duplication was cross-chart (a byte-identical `findPersonInTree`, a copy-pasted placeholder-extraction tail, and ~16 box-render helpers triplicated across the three `.vue` components). Shipped in five phases: **A** — deduped `findPerson` (3 copies → the existing unused shared one) and extracted a shared `extractPlaceholders`; **B** — a `useChartBox` composable holding the ~16 render helpers, adopted by all three components; **C** — a shared `<ChartCanvas>` component owning the SVG shell + pan/zoom + interaction, so render parity is now *structural* (one box renderer, three charts); **D** — behaviour parity leveled up to the most complete chart: double-click re-root (was Hourglass-only) now in all three via the host's `setTreeSubject`, selection auto-pan unified into ChartCanvas, `role="tree"`/`treeitem` + box keyboard focus + the `focusedPerson` external-focus path (which a code review caught Task 8 had turned into a dead write) restored across all three, and natural-orientation arrow-key navigation (Pedigree → = ancestors; Hourglass ↑ = ancestors / ↓ = descendants; Descendant ↓ = descendants) via a shared `useChartKeyboardNav`. The three layout *algorithms* deliberately stay separate files (legitimately different shapes). **Key deltas:** a Task-7 review caught a hardcoded `data-testid="pedigree-svg"` on the shared canvas (parameterized to a `testId` prop) + a no-op `addBtnStyle` alias; Hourglass's selection auto-pan lost its smooth-scroll easing (now instant, consistent across all three); Pedigree's arrow-nav now targets the cross-axis-*nearest* box vs first-in-array (directional semantics preserved); Hourglass keeps `focusedPerson` declared-but-unbound per the single-focus invariant (Bengt R50); the Coachmark anchor was re-resolved by querying the canvas's scroll element. Verified by a new `tests/components/chart-parity.test.ts` (14/14 — mounts the shared canvas, asserts every shared behaviour + arrow-nav orientation resolution + that all three delegate to ChartCanvas) and the unchanged 130-test `chartLayout.test.ts` property suite (layout maths behaviour-preserving). Visual parity confirmed on the running app (22k-person `test.db`, focal Bengt Gunnar Persson) across all three charts via the dev-MCP bridge. Close-out evidence: `npm test` 4443 passed (the lone failure was a missing MCP-sidecar build artifact in the fresh worktree, green after `build:mcp-sidecar`); `npm run build` exit 0 (release bundle in 1m24s, `.app`+`.dmg`); `npm run test:e2e:full` 174 passed (2.7m) across all 7 projects; lint 0 errors. — [plan](2026-06-07-chart-consistency.md) · [design](2026-06-07-chart-consistency-design.md)

### Hourglass layout internal refactor — authored, deferred, superseded (2026-05-14 → 2026-06-07)
A design-only "fuse" plan to split `hourglass.ts` along internal concerns, explicitly contingent on a real chart-feature trigger that never fired. The 2026-06-07 re-audit confirmed the premise was wrong (size ≠ the problem; cross-chart drift is) and the chart-consistency plan above subsumes its genuine value (shared `findPerson` + `extractPlaceholders` + `ChartCanvas`). The design file was deleted rather than archived-as-done — it was never executed. — superseded by [chart-consistency](2026-06-07-chart-consistency.md)

### Instant updates on large databases (2026-06-17)
User goal: editing anything (a name, date, place) reflects in every open view — list, side panel, chart/map/timeline — within a moment even on a tens-of-thousands-person database; no multi-second lag, no view stale until navigate-away-and-back. Triaged from the `[reactivity]` e2e failure: on the 22k-person `test.db`, a single rename took ~5 s to show in the family-tree chart. **Profiled root cause (T01, dev MCP):** the chart reactivity was *correct* — every `data-changed` fired two full-DB aggregate scans from `App.vue`'s badge handlers (`checks.runAll()` >5 s, `duplicates.count()` ~4 s), which serialized on the single SQLite `Mutex<Connection>` and starved the cheap visible-view reloads (chart/list/panel reload in <1 s). A burst of edits (e2e seeding, fast manual editing) left these scans running and starving everything. **Fix (T02, store-based):** the Quality and Duplicates sidebar badges now derive from the views' own computed counts via Pinia stores (`useQualityStore` already existed; added `useDuplicateCountStore` written by `PersonsTab`'s `usePagedList.total`) instead of `App.vue` re-scanning — zero redundant DB work, no scan on the per-edit path or on `/quality`·`/duplicates` entry. `quality_issue_counts` cache refresh moved into `QualityView.runChecks` so the Persons-list "Kvalitet" column stays correct. A first nav-trigger attempt regressed `[duplicates]` (the nav scan raced the view's own load); the store approach replaced it. **Key finding:** all four originally-failing e2e specs — `[reactivity]`, `[panels]` Media→Source link, and both `[imports]` (gramps-gpkg, genney-gcc) — were the *same* contention, and all went green with this one fix; the separately-filed [packaged-app-import-and-link-fixes](packaged-app-import-and-link-fixes.md) plan was thereby superseded (the importer "bugs" were never logic bugs — the MCP/Node import path was always correct). Close-out evidence: `npm test` 4449 passed; `npm run build` exit 0; clean `npm run test:e2e:full` **175 passed, 0 failed, 0 flaky** across all 8 projects. New `tests/components/App.test.ts` asserts no `runAll`/`duplicates.count` on mount. — [plan](2026-06-17-instant-updates-on-large-databases.md)

### Closed without execution — packaged-app import & panel-link fixes (2026-06-17)
Filed during the 2026-06-17 e2e-failure triage to fix three Tauri-path reds (gramps-gpkg absolute `file_ref`, genney-gcc sidecar "No such file or directory", Media→Source reciprocal link) via the rebuild-iterate loop. **Closed unexecuted the same day:** all three were contention-induced flakes, not logic bugs — the Gramps importer core produced a correct relative `file_ref` when run via the MCP/Node path on a clean DB, isolating the failures to load-under-contention. The [instant-updates-on-large-databases](2026-06-17-instant-updates-on-large-databases.md) fix (per-edit full-DB scans off the critical path) de-saturated the single SQLite connection and all three specs went green with zero code change here — clean `npm run test:e2e:full` 175 passed / 0 failed / 0 flaky across all 8 projects. A reminder that "deterministic-looking" e2e assertion failures (an absolute-path assert, a file-not-found) can still be timing artifacts when the renderer↔Rust IPC is saturated. — [plan](2026-06-17-packaged-app-import-and-link-fixes.md)

### Export stays fast and responsive on large trees (2026-06-17)
User goal: exporting a large family tree (GEDCOM, website, archive) finishes fast, shows progress while it runs, and never silently stalls — and stays that way (a reintroduced per-row query fails CI). **Profiled root cause (dev MCP, 22k-person `test.db`):** GEDCOM export did not complete in 3 min at ~0% CPU on every process — an IPC-round-trip stall from ~150k per-entity `await get*(db,id)` calls in the per-concept emitters (`emitNotesForEntity`/`emitPersonAssociations`/`emitNameTranslations`/`emitPlaceTranslations`), the exact case `.claude/rules/performance.md` flagged as "extend ExportPrefetch, don't cache in the emitter." **Fix:** extended `prefetchExportData` with bulk notes/associations/translations/coverage Maps (replicating each getter's ORDER BY for byte-identical output) and threaded them into the emitters (the `emitNegationsForEntity` optional-prefetch+fallback shape) — query count **40 020 → 27** on a 5k seed, gated by new `tests/unit/export-perf.test.ts`. Added `onProgress` to all three exporters wired to the export UI (mirroring the import fan-out), a `makeYieldBudget` on the website gazetteer loop, and fixed a pre-existing renderer bug where `exportGedcom` was called with an options object instead of positional args (UI silently ignored the 7.0 selection + export options). Durable guardrails: the query-count test, a "Responsiveness budget" section in `performance.md`, and a read-only `performance-reviewer` agent wired into `subagent-handoff` (B7). **Verification deviation (recorded honestly):** the absolute "finishes in seconds" wall-clock target is NOT claimed — a live debug-build measurement stayed slow at ~0% CPU and localized the residual cost to bulk `SELECT *` payload transfer through the db-shim (a separate, out-of-scope bottleneck, confounded by debug build + single-connection contention with the live app); it moves to the follow-up [export-bulk-transfer-cost](2026-06-17-export-bulk-transfer-cost.md), whose §1 IS that target. Close-out evidence: `npm test` 4456 passed; `npm run build` release exit 0 (1m28s); `npm run test:e2e:full` 174 passed + 1 flaky (`[duplicates]` four-tab — passed on isolated retry; the same contention-flake pattern documented above, unrelated to export). — [plan](2026-06-17-export-stays-fast-and-responsive.md)

### Exporting a 20k+ tree finishes in seconds, not minutes (2026-06-17)
User goal: a researcher with 20 000+ persons clicks Export GEDCOM and it finishes in seconds, not minutes. The follow-up to [export-stays-fast-and-responsive](2026-06-17-export-stays-fast-and-responsive.md), whose §1 wall-clock target was deferred here. **Rule-Zero measurement first (T01/T02):** built a clean timing harness — temporary `db_all`/`db_get` round-trip timing in `db-shim.ts` + a `window.__perfExportTiming` binding that drives the real `exportGedcom` with no save dialog and returns only metrics — and drove it against `export-import/test.db` (22 243 persons) in a **release** build via dev-MCP `ui_eval`. The measurement **falsified the plan's own bulk-transfer hypothesis**: of a 131 s export, **130.5 s (99.5 %) was a single query** — the un-paged `listPersons`'s per-row correlated `EXISTS` subquery (`livingSqlExpr`) projected over all 22 243 persons, O(persons × events). Every genuine bulk-transfer query was fast (200 774 rows across the db-shim IPC boundary in < 600 ms; emit loop 279 ms), and the CPU signature was Rust-host 100 % / WebView idle — `rusqlite` evaluating the subquery, not `serde_json` transfer. (The sibling's ~0 %-CPU / I/O-bound reading was a debug-build + concurrent-app artifact, corrected here.) **Fix (T03):** `listPersons` now derives `living` via the bulk set-membership helper that already existed in `personLiving.ts` (`loadLivingDerivation` — two O(events) queries — + `isLivingDerived`, the exact JS equivalent of `livingSqlExpr`), instead of the inline correlated subquery; the paged `listPersonsPage`/`getPerson`/`searchPersons` keep `livingSqlExpr` (≤ a page of output rows, trivially fast). **Re-measured (T04):** GEDCOM 5.5.1 export 131 260 ms → **904–967 ms** (≈ 135× faster), `gedBytes` identical (17 481 504) before/after — byte-identical; `persons.list()` 70 s+ → 237 ms (same root cause, the T02 sub-question). No db-shim/columnar/transport change was warranted. Baseline: `docs/baseline-perf/2026-06-17/export-bulk-transfer.md`. Close-out evidence: `npm test` 4456 passed (46s); `npm run lint` 0 errors; `npm run build` full bundle exit 0; `npm run test:e2e:full` 175 passed (2.6m) across all 8 projects. — [plan](2026-06-17-export-bulk-transfer-cost.md)

### Place resolution accuracy (2026-06-18)
User goal: real places in a tree resolve to the correct location and aren't flagged "ambiguous" when the resolver actually knows the answer. Triaged from 15 user-reported bad map pins, reproduced against the live `family.db` (the user's exact enabled-gazetteer set) with `scripts/check-reported-places.ts` and collapsed into **5 root causes, not 15 bugs**. **RC1 — historical beats modern:** `world-historical` nodes sit shallow under `World (Historical)` and the depth tiebreaker preferred them, aggravated by `lang-world-historical` attaching modern Swedish exonyms as aliases ("Iran" on Qajar Iran, "Spanien" on Spanish Empire, "Edum" on Edom, "New" on Estado Novo). Fixed two ways: a scoring signal (`findMatches` tags `isHistorical` + `matchedHistoricalByOwnName`; `isBetterCandidate` + `pickBest` down-rank a historical candidate matched only via a modern alias, so a modern match always wins unless the user named the historical entity by its own name) **and** a deterministic offline data cleanup (`scripts/clean-historical-aliases.ts` — `buildModernNameSet` + `cleanHistoricalAliases` (node aliases) + `cleanTranslations` (the language-gazetteer exonym map); removed 189 node aliases + 289 exonyms; folded into both build scripts so a SPARQL regen stays clean). **RC2 — spurious "ambiguous":** `pickBest` flagged ambiguous on any same-quality coord difference; rewritten so ambiguous means a runner-up ties best on the semantic signals, is the same granularity (depth within 1 — a country vs a hamlet is a decisive scope win), and is a genuinely different place (not the same pin from a point+boundary gazetteer, not a province-vs-its-city stem/leaf). **RC3 — ISO codes as sub-tokens:** `world-countries` carries alpha-2/alpha-3 aliases (`SN`,`BY`,`TUN`); token-scan matched "sn" (socken)→Senegal, "by" (village)→Belarus, "Tun"→Tunisia. Now code aliases (2–3 all-caps raw) route to a separate `normCodeAliases` set matched only as a whole comma-component, never a sub-token. **RC4/RC5 — namesakes & coverage gaps:** "Kärret, Hov" (real Hovs in Sweden ×3 + Faroe, no country hint) is now honestly flagged ambiguous rather than confidently pinned to the Faroes — forcing Sweden would be render-time inference, forbidden by the Prime Directive; Warszawa resolves to Poland (partial), no city data shipped. Verified by a 15-case corpus (`tests/unit/place-resolution-accuracy.test.ts`) locked red-first, the real-DB re-audit, and 3 independent reviews (spec ✅, performance ✅ — index work is build-time/cached, ambiguity loop O(candidates×depth-bounded); code-quality found a latent `semanticTie`-missing-historical-dimension bug + a zero-name-read-invariant regression, both fixed). Design decisions taken with the user: ambiguity = "only true ties"; historical = scoring + data cleanup; ISO codes = whole-component-only. Close-out evidence: `npm test` 4464 passed (304 files); `vue-tsc` 0 new errors in changed files; `npm run build` release exit 0 (1m24s, `.app`+`.dmg`); `npm run test:e2e:full` 174 passed + 1 flaky (`[duplicates]` four-tab — passed on isolated retry, the documented contention-flake, unrelated to place resolution). — [plan](2026-06-18-place-resolution-accuracy.md) · [design](2026-06-18-place-resolution-accuracy-design.md)

### Merged gazetteer carries normalize rules (2026-06-18)
User goal: Swedish place strings that use a suffix or genitive — "Ängby, Stockholms kn", "Mo, Bergs kn" — resolve to the correct Swedish place, not a stray ISO-coded country (Saint Kitts via "KN", Macao via "MO"). Immediate follow-up to [place-resolution-accuracy](2026-06-18-place-resolution-accuracy.md): three further reported cases reproduced on the just-shipped code and traced to a **pre-existing** root cause the prior plan's ISO sub-token gate didn't reach — `loadGazetteers` emitted the synthetic `__merged__` gazetteer (the single tree the app resolves against) with `normalize: undefined`, dropping every source's `stripSuffixes`/`stripPrefixes`/`stripPatterns`. So in the merged tree "Stockholms kn"/"Bergs kn" never reduced to "Stockholm"/"Berg", no Swedish node matched, and the stray "kn"→Saint Kitts / "Mo"→Macao code-alias became the only candidate (it worked in the *per-source* index, masking the bug). **Fix:** the merge now sets the merged gazetteer's `normalize` to the union of all non-language sources' rules. **Perf cliff caught + fixed mid-execution:** the union (large all-locale rule set) exposed that `normalizeForGazetteer` re-ran `normalizeUniversal(suffix)` per suffix and recompiled every `stripPattern` regex per node at index-build — O(rules×nodes) regex work that pushed resolver unit tests past the 5 s vitest timeout (7 timeout-failures in the full parallel run, all green in isolation); fixed by caching the normalized/compiled rules once per gazetteer in a WeakMap (semantics unchanged, heavy suites back to ~7 s). Also fixed two pre-existing `readonly`→`string[]` type errors in `resolvePlace`/`resolveBoundary` (`matchedPath` now returns a mutable spread copy of the cache-owned path — type-correct and protects the index cache from caller mutation). **Out of scope (documented):** "Boston, USA" → United States but a tiny Boston (OR), because there is no Boston, MA in the bundled gazetteers — a coverage gap needing city-level data, asserted only as "stays in the US"; bare single-token ISO collisions ("Mo"/"kn" alone) remain genuinely ambiguous. Verified by two new corpus cases (red-first) + the existing 15-case corpus + the real-DB re-audit. Close-out evidence: `npm test` 4466 passed (304 files); `vue-tsc` resolver.ts clean (the two pre-existing errors fixed; remaining errors are pre-existing config/test-file noise); `npm run build` exit 0 (`.app`+`.dmg`, after a two-concurrent-builds-in-one-worktree collision was identified and a clean single build run); `npm run test:e2e:full` 175 passed, 0 flaky across all 8 projects. — [plan](2026-06-18-merged-gazetteer-normalize.md)
